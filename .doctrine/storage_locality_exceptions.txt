# STORAGE-LOCALITY exceptions — verbatim path<TAB>reason.
#
# A listed path that no longer contains the pattern is ALSO a breach: this list
# describes what is there, not what is forgiven in general.
crates/reasonbraid-server/src/authority/transaction.rs	subsec_nanos() measures a Duration's sub-second remainder to reject sub-millisecond limits; it names nothing and reaches no path
scripts/tests/test_ci_env.py	the probe program this test BUILDS AS A STRING calls tempfile with no dir= on purpose: test_exec_uses_relocated_root_and_local_temporary_file exists to observe where the launcher makes a child's temporary file land, and pinning dir= would assert the answer instead of measuring it
