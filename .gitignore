# Rust build output
/target
**/*.rs.bk

# Commit workflow scratch (must stay untracked — see COMMIT.md)
git_message_brief.txt

# Generated artifacts are regenerated locally, never tracked (see COMMIT.md)
/generated
# mdBook HTML output (`make book` → docs/book/book/); the source is docs/book/src/
docs/book/book/

# Logs & OS cruft
*.log
.DS_Store

# The release identity key (`.2.3`, ADR-027): the releaser's local PKCS8 —
# never tracked; the signed manifest + the signature live under target/.
release-key.pk8

# Note: Cargo.lock is intentionally NOT ignored — binary/application projects should
# commit it for reproducible builds. If this becomes a library-only workspace, add it.
