# DOCTRINE_ENFORCEMENT.md — how every mechanizable doctrine is enforced

Discipline holds because it is **mechanical**, not remembered. A rule that lives only in a
doc is a suggestion; a rule wired into a git hook + CI is enforced for every agent and
every human, identically.

## Defense in depth (four layers)

- **E1 — discovery.** The doctrine docs: this file, `README.md`, `MEMORY_ARCHITECTURE.md`,
  `TOOLBOX.md`, `COMMIT.md`, and `docs/decisions/`. Where an agent learns the rules.
- **E2 — self-check.** `scripts/check_doctrines.sh` (the driver) + each registered
  `scripts/check_*.sh`. The single source of truth for "which doctrine is enforced by
  what". Runnable by hand anytime.
- **E3 — git hook.** `.githooks/pre-commit` calls the enforcer; `.githooks/commit-msg`
  checks the subject shape. Activate once per clone: `git config core.hooksPath .githooks`.
- **E3b — pre-push hook.** `.githooks/pre-push` runs `cargo deny check`. ⭐ It is here
  and not in `pre-commit` because of what it is TRIGGERED BY: a commit cannot introduce
  an advisory, but an advisory published overnight applies to a tree nobody touched. So
  the dependency gate is **time-triggered**, and a push — the moment a claim leaves this
  machine — is when the graph must be re-asked. ⛔ This is NOT the full CI run: it does
  not build, test or lint; `COMMIT.md` owns that discipline. 1.15 s with the advisory
  database cached under the repository-local `CARGO_HOME`
  (`SIGNOFF-REPAIR.11.4.7.2.3`).
- **E4 — CI.** The same enforcer runs in CI (`.github/workflows/doctrines.yml`), so a
  locally `--no-verify`'d hook still fails the build. This is the "no matter what" backstop.
  ⚠️ **Stated plainly because it was the cause of a real failure:** this backstop has
  never run (blocker C1). Two supply-chain gates lived ONLY here and were therefore
  executed by nothing at all — `cargo deny` was red on RUSTSEC-2026-0285 and `gitleaks`
  on a `generic-api-key` finding, both unreported, until `SIGNOFF-REPAIR.11.4.7.2` ran
  them by hand. E3/E3b now run both locally, so E4 is a backstop again rather than the
  only stop.

## The enforcer registry

`scripts/check_doctrines.sh` carries the **universal** registry:

| ID | Proves | Check |
| --- | --- | --- |
| `MEMORY-ARCH` | the durable 4-layer memory invariants hold | `scripts/check_memory_architecture.sh` |
| `DOCPATH` | tracked `.md` carry no checkout-specific absolute paths | `scripts/check_docpaths.sh` |
| `TASK-TREE-OWNERSHIP` | every staged code change is owned by a task-tree leaf | `scripts/check_task_tree_ownership.sh` |
| `TASK-ACCEPTANCE` | a staged **code** change is owned by a task-tree leaf whose acceptance checklist has ROOT CAUSE / ADDRESSED / NO REGRESSION **ticked**, each backed by tool output **inside that box's own bullet**. ⭐ Box-scoping is the soundness property, not a nicety: it closes two measured leakage holes — a co-staged unrelated leaf supplying the evidence, and a token matched anywhere in the file. ⚠️ Honest limit: it proves the author cited something re-runnable, never that the output is true — the un-fakeable leg is re-running the cited command in CI. Project seams in `.doctrine/` keep it neutral | `scripts/check_task_acceptance.sh` |
| `WAIVER-ROUTING` | a task leaf saying a gate **does not apply** names the leaf that owns fixing it — ⭐ *an author writing a waiver IS the gate reporting a missing capability*, the highest-signal defect report a gate can receive. Deliberately does **not** punish honesty: the waiver stays legal, it just has to name an owner | `scripts/check_waiver_routing.sh` |
| `README-STABILITY` | `README.md` stays a stable landing page — **derived** line AND byte caps (a line cap alone is measurably bypassable: a real project running this spine passed its 60-line layer-A cap while carrying 138,403 bytes), plus **routing-pressure closure** (every destination the README, the policy, or the guard's hint name ends at a governed terminal in `.doctrine/readme_routes.txt`) and the CHANGELOG append-only-history rotation threshold | `scripts/check_readme_stability.sh` |
| `LIVE-DOC-CURRENCY` | no tracked document reports its own currency (`Last updated:` and kin) — git already carries it, and a hand-kept date is right the day it is typed and false the day after; the upstream instrument that scores distinct dates per live surface against a declared charter is a backlog item | `scripts/check_live_doc_currency.sh` |
| `LESSON-PROMOTION` | a NEW dated lesson heading staged in `DEV_NOTES.md` must be either **promoted** (a `docs/knowledge/` change, or a `docs/decisions/` record gaining `answers:`) or **explicitly declined** (`promotion: declined (<reason>)` in the owning leaf) — never silently dropped. Founding measurement upstream: 1 592 lesson entries, none reachable by question, because no gate asked. Evidence archetype: it verifies a decision was RECORDED, not that it was right | `scripts/check_lesson_promotion.sh` |
| `ROUTING-EVIDENCE` | a task leaf that routes a finding **out to another tree** carries a `ROUTING EVIDENCE` section: does the finding reproduce OUTSIDE the family it is sent to, what was measured, what would make the routing wrong. Keyed on the semantics of leaving the tree (the first cut upstream, keyed on a tree-ID spelling, missed its own founding incident); intra-tree routing is not flagged | `scripts/check_routing_evidence.sh` |
| `GAP-CLAIM-CENSUS` | a task leaf that **ADDS** a *"nothing checks X"* claim records the CENSUS it rests on in the same heading section (a command that enumerates a population, or `census: not run (<why>)`). Such a sentence is a universally quantified claim over the whole tree, false the moment one reader exists; staged-diff-scoped (81 pre-existing claims upstream would otherwise teach bypass); `--all` reports the backlog, advisory ⛔ **Its section scanner did not know what Markdown is** (`SIGNOFF-REPAIR.11.18`): it called every `^#{1,6} ` line a heading, so a shell comment inside a fenced CENSUS opened a pseudo-section and the claim above it lost the census three lines below — which is exactly how it BLOCKED `.11.14.3.2`. ⭐ The direction was MEASURED rather than assumed: a pseudo-section is strictly INSIDE its real one, so the defect could only SUBTRACT censuses from a claim — **false positives only**, and the leaf's own suspicion of a silent false negative is REFUTED by a fixture that classifies identically before and after. Fences now toggle (``` and `~~~`, up to 3 spaces of indent), an indented ATX heading is a heading (one in the corpus, in the TEMPLATE file), and ⚠️ **an UNBALANCED fence REFUSES rather than guessing** — otherwise every later claim silently inherits the last real section's discharges. Population unchanged: **94 claims, 0 unbacked**, before and after ⭐ **THE ADVISORY AND THE BLOCKER NOW GOVERN THE SAME CORPUS, AND FOR THIS GATE'S WHOLE LIFE THEY DID NOT** (`SIGNOFF-REPAIR.11.18.1`). `--all` iterated `git ls-files 'docs/tasks/*.md'` — **69** files, because git's `*` crosses `/` — while the blocker filtered `^docs/tasks/[^/]*\.md$` and saw **15**. The backlog therefore reported **102 claim lines across 6 files** while enforcement covered **97 across 5**, with **54** nested artifacts invisible to the blocking path. ⛔ The cause was that the script spelled the population TWICE, once per path, with the `TEMPLATE.md` exclusion inside the staged loop where `--all` could not reach it; one `governed_filter` now serves both, so the agreement is structural rather than remembered. ⭐ The *a signoff artifact is a dated record, not a living leaf* defence was REFUTED by measurement — **66 of 559 commits** modify one — and the extension is free: replayed over **all 559 commits** it would have blocked **0**, `REASON-CODE-DOC`'s shape. ⛔ Full history rather than a window, deliberately: the same harness over the TOP-LEVEL corpus also returns 0, and THAT 0 is survivorship, since the blocker has governed those files since the initial commit and every commit that landed had already been made to pass — `.11.18.2` owns the general statement | `scripts/check_gap_claims.sh` (`--self-test`) |
| `TABLE-ARITY-RATCHET` | a staged `.md` may not RAISE the number of table rows whose cell count disagrees with their header — GFM silently DROPS extra cells and PADS missing ones, so the page looks fine and the reader loses the rightmost column (26 of 197 rows of a shipped contract upstream, every enforcer green). Per-file ratchet against HEAD. ⛔ **Only a backslash-escaped `\|` is respected — an inline code span does NOT protect a pipe**, and the first implementation had it exactly backwards: it modelled the opposite AND its own `--self-test` asserted that same false answer, so the corpus read **0** defective rows where mdbook 0.5.2 — the renderer this project publishes its book with — rendered **2**. ⚠️ One of them was **this row**, which published 516 of its 1,027 rationale characters and a bare `—` where its enforcer name belongs, and was deliberately retained as the falsification target. Every `--self-test` arm is now the RENDERER's verdict, rendered before being asserted, and the banner COUNTS the arms rather than restating a literal — it read `9/9` while nine were written, so a tenth would have published a false total (`SIGNOFF-REPAIR.11.16`) | `scripts/check_table_arity.sh` |
| `HEADING-DEPTH` | no tracked Markdown carries an ATX heading deeper than level 6. ⭐ `#######` is not a level-7 heading — it is a **paragraph that starts with hashes**, in CommonMark and GFM alike. A document that encodes hierarchy in heading depth therefore runs out of levels *silently*: the file keeps looking nested and every heading-aware reader stops seeing the structure. Measured when the gate was proposed, at `6bf0c40`: 29 such lines at levels 7–11, whose content was attributed to the nearest real heading — the first census written against that file reported one section holding 18 status lines when it held one and had swallowed five children. The remedy is not more hashes: a leaf id already carries the depth, and carries it further than six levels can. Fenced blocks exempt | `scripts/check_heading_depth.sh` (`--self-test`) |
| `TASK-STATUS` | a task-tree leaf carries exactly **one** `- Status:` line. The convention that grew instead was to APPEND a closing status and leave the opening one in place, so a section asserted both and the reader picked by accident — and any tool taking the first match, which is the obvious way to write it, took the stale one. ⚠️ Not hypothetical: `SIGNOFF-REPAIR.7.4.1` closed with a real commit and then sat at **frontier row 2 for seven further commits**, because the row was written from its first line. Five sections carried the contradiction. The remedy keeps every word — the opening line becomes `- Opened:`, which is what it always meant | `scripts/check_task_status.sh` (`--self-test`) |
| `LOCKSTEP-CLAIM` | a ticked `**LOCKSTEP**` box may not name a core live document the commit does not stage. The box is a CLAIM about THIS commit, and `TASK-ACCEPTANCE` cannot catch it: that gate proves the box is ticked and cites something re-runnable, never that the cited edit landed. ⚠️ Not hypothetical — `6bf0c40` shipped a ticked box naming `MEMORY.md` and `LIVE_STATUS.md` and staged neither, after a scripted multi-edit hit a failed assertion and `git add -A && git commit` ran regardless. ⭐ Keyed on the author's own claim rather than a blanket requirement, and the census is why: over 25 commits, 10 closed a leaf and `CHANGELOG.md` was staged 10/10 but `MEMORY.md` only 6/10 — a blanket rule would have asserted something the project does not do, flagged four innocent commits, and still missed the defect. Re-measured with the finished gate over 30 commits: 11 add a claim, 10 pass, exactly 1 breaches, zero false positives. Honest escape in the claim's own section, per the `GAP-CLAIM-CENSUS` precedent: `lockstep: <doc> unchanged (<why>)`. `--against <sha>` re-runs it over any past commit | `scripts/check_lockstep_claim.sh` (`--self-test`) |
| `INDEX-FRONTIER` | `docs/TASK_TREE.md`'s Frontier column may not name a leaf the owning tree's own row 1 does not. It is a SECOND copy of a fact each tree already owns, nothing derived it, and it drifted for **39 of the last 60 commits** — starting, precisely, at `1ebfebe`, the commit that CLOSED the leaf the row went on naming. `BOOK-FRONTIER` fixed this exact shape for the book and was not extended to the project's own index. ⛔ Deliberately NARROWER than "the cell equals row 1", because the census rejected that: 8 completed trees write `\| — \|` and point at the NEXT tree's first leaf, `PHASE-8`'s row 1 is a cross-tree prerequisite its cell summarises accurately, and 2 rows claim nothing — so equality would flag legitimate rows. Only an ACTIVE tree whose row 1 names its OWN leaf is checked, which is also the only row that moves. ⛔ A generator was the provisional preference and the census rejected it too: regenerating the column would destroy accurate curated prose in 13 of 14 rows. `--against <sha>` re-runs it over any past commit | `scripts/check_tree_index_frontier.sh` (`--self-test`) |
| `FRONTIER-STATUS` | a frontier row's status column agrees with the leaf it names, and row 1 is not a finished leaf. The two are copies of one fact and **drifted three times**: `.7.4.1` sat at row 2 for seven commits after closing (`.11.15`); REPAIR-0253 reconciled FIVE rows saying `pending` whose leaves said `done`; and two commits later **row 1 itself** named `.7.4.5`, `done` since REPAIR-0216 — the one row a fresh session resumes from (`SIGNOFF-REPAIR.11.22`). ⭐ **The mechanism is sharper than *nobody checked*:** the table gains a NEW row when a leaf closes and the opening row is never removed, so one leaf ends up with rows that contradict each other. Censused before the rule (`.11.6`): **66 rows naming 49 leaves, 13 leaves with 2–4 rows each, 10 rows disagreeing**. ⛔ Three gates look adjacent and every one correctly misses it — `INDEX-FRONTIER` compares the two FILES to each other (both agreed, about a closed leaf), `TASK-STATUS` reads leaves and never tables, `TABLE-ARITY-RATCHET` reads cell counts and never cell meaning; the defect lives in the seam. ⚠️ **A `done` row is explicitly LEGAL** — most of the table is closed rows kept as history, so the rule is that a row must not LIE about its leaf, not that finished leaves are banned. ⚠️ **A leaf's status is not always in a `Status:` line**: 350 leaves, 28 carry none and 27 of those state it in an `Opened:` bullet, so reading one form would report 28 false disagreements — which is how a gate gets switched off rather than fixed. `active` is a real third value and is not finished. 0.06 s | `scripts/check_frontier_status.py` (`--self-test`) |
| `RUST-FORMATTING` | the workspace passes `cargo fmt --all -- --check`. 🔴 **Nothing ran it per commit, and unformatted Rust reached `main` and stayed there** — `crates/reasonbraid-server/src/git.rs` failed at two sites (an over-split `\|\|` chain, an un-wrapped `assert!`) from REPAIR-0251/0252, found only because a LATER leaf happened to run the check while verifying a file it had not touched (`SIGNOFF-REPAIR.11.21`). ⭐ **A policy gap, not a lapse:** `CLAUDE.md` §16 routes full CI to pre-push and the per-commit gate to the enforcer, whose 18 checks contained no formatting check, while `cargo fmt --check` sat only in `COMMIT.md`'s pre-push list — and pushes go out in batches of ~300 commits, so unformatted code could sit on `main` for as many commits as that. Measured before proposing (`SIGNOFF-REPAIR.11.6`), three runs each: **0.81/0.66/0.66 s** against an enforcer at **12.57/12.52/13.00 s** — about 5 %, and nobody routes around two-thirds of a second (`SIGNOFF-REPAIR.11.5`). ⛔ The rejected alternative is leaving it pre-push: that is exactly the arrangement that let the defect ship, and a check running once per ~300 commits cannot say WHICH commit broke formatting. ⚠️ **Whole-workspace, not staged-scope, deliberately** — the defect was in a file no staged change touched, so a staged-scope check would not have found it; affordable only because the workspace is formatted as of REPAIR-0256, where a strict check over pre-existing debt would have needed a ratchet like `TABLE-ARITY`. ⛔ A missing `rustfmt` is ANNOUNCED, never passed over, and its self-test fails rather than skipping, because a silent skip looks exactly like a pass. Its refusal names repository-relative paths (§12) | `scripts/check_rust_formatting.sh` (`--self-test`) |
| `SELF-TEST` | Every check and census instrument passes its OWN `--self-test`. Until this gate, **nothing ran them**: they were author-run, fired once while the change was written and never again. `census_pg_test_clusters.py`'s citation guard asserted a probe name appears in ZERO tracked files while writing that probe as a literal in its own tracked source — it passed exactly once, in the working tree before the file was added, and failed from the commit that added it, unnoticed for dozens of commits (`SIGNOFF-REPAIR.11.4.3.1.7.1`). Measured before proposing (`SIGNOFF-REPAIR.11.4.3.1.7.2`): **17 self-tests, 1.01 s total**, against an enforcer costing **3.15 s** — one second on a three-second gate, which nobody routes around (`SIGNOFF-REPAIR.11.5`'s constraint). ⛔ It catches NOTHING today: every discovered self-test passes. Its value is preventing a control from silently stopping, not finding a present defect. ⚠️ Some tracked check/census instruments carry no `--self-test` at all and this does not reach them. 🔴 **Both of those populations used to be RESTATED here as live facts — "all 17 pass" and "11 of the 28" — and both had drifted**, to 29 and to 9-of-38, with nothing in the repository deriving either (`SIGNOFF-REPAIR.11.16`). `scripts/check_self_tests.sh --census` prints them on demand; the `17` above survives because it is ANCHORED to the leaf that measured it instead of claiming the present. ⚠️ **The gate fell into its own trap while being built**: registering it put the flag's literal into the enforcer's description, discovery found the ENFORCER, ran it with the flag, and the enforcer — which ignores unknown arguments — ran every check again. Unbounded recursion on the first `make gate`. Defended twice now: an exported `RB_SELF_TEST_GATE` makes a nested invocation exit immediately, so no discovery mistake can recurse, and the enforcer is excluded by path because it is the runner, not a check | `scripts/check_self_tests.sh` (`--self-test`) |
| `KNOWLEDGE-MAP` | the derived Knowledge Map is in sync (if the subsystem exists) | `knowledge-map/scripts/check_knowledge_map.sh` |
| `VISIBILITY-POLICY` | every tracked-Markdown sentence stating a **private** visibility for this repository is reviewed verbatim in `.doctrine/visibility_exceptions.txt`. ⭐ The director's public-repository correction leaked past two hand-run censuses; a census is a one-shot measurement of a moving corpus. A NEW or REWORDED sentence breaches, and so does a STALE exception — the allowlist must describe what is actually there. Matching is deliberately precise: an allowlist thirty entries long teaches bypass | `scripts/check_visibility_policy.sh` (`--self-test`) ⚠️ **AND THE NOUN IS ABBREVIATED TOO** (`SIGNOFF-REPAIR.11.2.4`): every clause was anchored on the full word, so two live instances written in the abbreviated copular form sat in the corpus invisibly — in a gate built for exactly that claim. The widening is DERIVED from a ~60-line census, not invented: the abbreviation is the only uncovered spelling that STATES a visibility, while the many `private-repository instruction` lines put `private` BEFORE the noun and are corrections of the superseded policy, so no clause matches them and none should. Delta 3 → 11 matches, none lost. 🔴 **Six of the eight new matches were the project DOCUMENTING its own blind spot** — `check_self_tests.sh`'s founding incident reached from the documentation side. Dispositioned by rule: except what must stay VERBATIM (3 preserved-evidence entries), reword what is PROSE (5 live sentences now NAME the form instead of instantiating it), so the changeable surfaces stay policed |
| `BOOK-FRONTIER` | the book may not hold a SECOND, unchecked copy of the task tree's frontier — its roadmap page named a leaf seven commits after that leaf closed. A page may name the frontier only if it matches row 1 of the owning tree's table; naming none passes, because routing to the per-leaf maintained page is the intended shape | `scripts/check_book_frontier.sh` (`--self-test`) |
| `FILE-TERMINATION` | every tracked text file ends with **exactly one** newline — no missing terminator, no blank line at EOF. ⭐ Deliberately NOT a `git diff --check` wrapper: `ROADMAP.md` uses trailing double-spaces as Markdown hard line breaks, so that check's whitespace family contains a legitimate use here and a blanket rule would teach bypass. A blank line at EOF has no legitimate use, and is the exact defect that reached a commit and forced REPAIR-0062. Whole-tree, so the CI backstop is real, and the population scans in well under a second — `scripts/check_file_termination.sh --census` prints its size, because the **642** once written here had become 773 with nothing deriving it (`SIGNOFF-REPAIR.11.16`). Exceptions are verbatim in `.doctrine/file_termination_exceptions.txt` — generated or digest-bound bytes only — and a stale entry is also a breach | `scripts/check_file_termination.sh` (`--self-test`) |
| `FIXTURE-PLAN-CHILDREN` | a test fixture's cleanup plan that deletes a parent must also delete every table holding a foreign key to it. ⛔ The shared runtime checker already enforces this and only runs when the suite RUNS: `migrations/0062` added `evidence_citations` with a key to `tenants`, swept only the plans naming its other parent, and **six suites could not start for roughly twenty commits** because nothing executed them — then `migrations/0067` repeated it in the session that wrote the rule down. This is the commit-time trigger, and it needs no database because every foreign key in this corpus is inline in a `CREATE TABLE`. ⚠️ It **refuses rather than under-reports**: an `ALTER TABLE … ADD CONSTRAINT … FOREIGN KEY` it does not model, or a `delete_tables(` call shape it cannot read, is an error rather than a skipped edge — the helper's own definition and its negative-control suite are the only exclusions, by path and with the reason | `scripts/check_fixture_plan_children.py` (`--self-test`) |
| `POSITIONAL-REF` | a positional source reference in tracked Markdown — a source path, a colon and a line number — names a file a reader can FIND. ⛔ `docs/CLAIM_VERIFICATION.md` §4.1 grades a NAMED INSTANCE as exact with no tolerance band, and a BARE BASENAME is exact only when it names ONE tracked file — a bare `profiles.rs` with a five-digit line named a 606-line source AND an 11,154-line suite, and the prose did not say which. 🔴 **The gate flagged THIS ROW on its first run**, because the sentence originally quoted that reference verbatim; it is fixed by RE-WORDING rather than by excluding the registry, so the doctrine polices its own description — the `STORAGE-LOCALITY` founding incident, again. ⭐ `DOCPATH` already wants repo-root-relative references and does not reach this: a bare basename SATISFIES it while naming nothing, which is `BOOK-LINKS`' founding shape exactly — *the doctrine's intent was satisfied and the navigation broke*. ⭐ **Calibrated across 200 commits BEFORE it was proposed** (`.11.6`): of **415** positional references added, **48 were ambiguous (11.6%)** and it would have blocked **19 commits (9.5%)** — against the **87%** and **93%** that got `.11.9`'s and `.11.15`'s candidate gates rejected for teaching bypass. The standing population (**29 distinct / 47 occurrences**) was discharged to **0** first, so it fires only on the next one. ⛔ It gates AMBIGUITY, not DRIFT: which FILE is fixable mechanically and stays fixed, while a line number moves with every insertion above it and could only be gated by re-verifying every line on every commit. ⚠️ It governs the dated ledgers too, because adding a path does not rewrite a record — it states the same thing more precisely. ⭐ **SECOND ARM: an UNRESOLVED reference is refused as well** (`SIGNOFF-REPAIR.11.17.1`) — a basename naming NO tracked file is not resolvable at all and is indistinguishable from a typo. ⛔ The obstacle was real and is answered rather than waived: a gate on this class would have to tell an illustrative EXAMPLE from a CITATION, which no instrument can do from a basename. It does not try. An example may not be WRITTEN in the positional form, and this row's own `file.rs` placeholder was REWORDED rather than excluded — the third time this doctrine has policed its own description, after the `STORAGE-LOCALITY` founding incident. ⭐ The eight citations that opened the class were never a deleted file: they were `gix-0.87.1/src/config/cache/init.rs`, a DEPENDENCY, and every cited line was exact at the pinned version. Crate-and-version qualified, such a reference resolves AND dates itself, which a bare one never could. ⭐ Calibrated across 200 commits before the arm was proposed (`SIGNOFF-REPAIR.11.17.1`): it would have blocked **2 commits (1.0%)**, and both are the two instances this leaf discharged — zero false positives, `REASON-CODE-DOC`'s shape. ⭐ **THIRD ARM: A SLASH IS NOT A RESOLUTION** (`SIGNOFF-REPAIR.11.17.2`), and it closed this row's own DECLARED LIMIT. Until it, a reference containing a slash was `pathed` without consulting the tracked tree — resolvability read off the SHAPE of the string, while the same instrument consulted the tree for every reference WITHOUT one (`docs/knowledge/trust-comes-from-the-check-not-the-shape.md`). 🔴 Of **251** such occurrences at `6f91897`, **39** named no tracked file and **one was a suffix of THREE** — the exact ambiguity this gate exists to refuse, passed by it. Re-derive with `python3 -B scripts/census_positional_refs.py --at 6f91897`. ⭐ A path is now asked the SAME question as a basename — *how many tracked files can a reader reach?* — which produces the classes rather than being argued into them: `partial` (a path-suffix of exactly one) is ACCEPTED like `unique`, several is `ambiguous`, none is `unresolved`. ⛔ The suffix must fall on a SEGMENT boundary, or the founding 93-false-positive hyphen bug returns as a false NEGATIVE. ⭐ **A DEPENDENCY CITATION IS A CLASS WITH AN ORACLE**, not an exemption: `Cargo.lock` says whether the graph holds that `<crate>-<version>`, so the class earns a FAILURE MODE with its name — a `dependency-stale` citation into a version the project no longer builds, which nothing said before. ⛔ It does NOT verify the in-crate path: that needs the vendored registry a cold clone lacks, so the check would pass here and fail there — `SIGNOFF-REPAIR.11.5`'s constraint. ⭐ **Calibrated before it was proposed** (`.11.6`): the three refused classes would have blocked **21 of 200 commits (10.5%)**, against the **9.5%** that argued this gate in and the 87%/93% that got `.11.9`'s and `.11.15`'s rejected. ⚠️ Refusing `partial` too was priced at **4.5%** and DECLINED anyway — on SHAPE, not cost: a partial path naming one tracked file resolves, and refusing it gates tidiness rather than ambiguity. The standing population (**3 ambiguous + 5 unresolved**) was discharged to **0** first, so it fires only on the next one | `scripts/census_positional_refs.py --check` (`--self-test`) |
| `BROKEN-TABLE` | a tracked Markdown table's BOUNDARY is where the author put it — no blank line INSIDE a body, and no block ABUTTING one without a blank line. ⭐ A blank line ENDS a GFM table, so every row after it renders as a paragraph of literal pipe-text instead of a row — and the SOURCE looks perfectly fine, which is why this survived every review that read the file rather than the page. 🔴 It was live in the active tree's OWN Current Frontier table, orphaning every one of its rows including row 1 — the cell `INDEX-FRONTIER` and `BOOK-FRONTIER` both exist to police (46 of them at `358ee13`, where this was measured). ⛔ `TABLE-ARITY-RATCHET` governs these same files and CANNOT see it: that gate compares a row's cell count against its header's, and an orphaned row has no header to disagree with — `BOOK-LINKS`' founding shape a third time, *the doctrine's intent was satisfied and the rendering broke*. ⭐ Every parsing rule is the RENDERER's answer rather than the specification's, the practice `TABLE-ARITY-RATCHET` established here: a delimiter row whose cell count differs from its header is NOT A TABLE, four spaces of indent is a code block, three is still a table, and a blank line mid-body produces one paragraph rather than a second table — each rendered before being asserted, and each an arm. ⚠️ An UNBALANCED FENCE REFUSES rather than guessing, per `GAP-CLAIM-CENSUS`'s lesson. ⭐ Calibrated across 200 commits before it was proposed (`SIGNOFF-REPAIR.11.19`): it would have blocked exactly **1** commit (**0.5%**), and that commit is the one that INTRODUCED the defect — zero false positives over the window, which is `REASON-CODE-DOC`'s shape rather than the backlog shape `SIGNOFF-REPAIR.11.9` was rejected at. The standing population was discharged to 0 first, so it fires only on the next one; `python3 -B scripts/census_broken_tables.py` prints the population, because `SIGNOFF-REPAIR.11.16` measured what happens to a number restated where nothing derives it. ⭐ **THE MIRROR IS THE SECOND ARM** (`SIGNOFF-REPAIR.11.19.2`): with NO blank line, the table SWALLOWS the block after it and every line renders as a row padded to the header's width — `PHASE-3`'s closing *"Tree complete"* paragraph was eleven such rows, and its table had no rows of its own at all. ⛔ Which constructs end a table without a blank line is NOT uniform and could not be read off a specification: a list item, a heading, a blockquote and an HTML block DO; plain prose and indented continuation text do NOT, and reading a list as absorbed over-counted that population by a third on the first pass. ⭐ The second arm was calibrated over the FULL history rather than a window (`SIGNOFF-REPAIR.11.19.2`): **19 of 551 commits (3.4%)** would have been blocked and **every one of the nineteen introduced a defect this work has since repaired** — zero false positives across the project's whole life | `python3 -B scripts/census_broken_tables.py --check` (`--self-test`) |
| `STORAGE-LOCALITY` | project-owned data lives on the **repository's own volume**, and a name is proved by exclusive creation rather than proposed by a clock. Four patterns, one policy. In Rust: `env::temp_dir()` — measured on a DIFFERENT volume from the checkout, so data written there leaves the repository's storage accounting — and `subsec_nanos()`/`as_nanos()`, measured at 501 distinct values in 2000 calls with every collision between ADJACENT calls, which is exactly the concurrent case. In shell and Python since `SIGNOFF-REPAIR.11.2.2`: a temporary directory with no repository-derived template, and `tempfile.*` with no `dir=`. ⭐ That extension has the sharpest founding case the project has produced — **three scripts that ENFORCE doctrine were writing their own scratch to an ambient TMPDIR, and two probe scripts `git init` whole throwaway repositories there**, none of which this gate could see because it enumerated `*.rs`. It fires on **0** today and would have fired on all **7** (contrast `SIGNOFF-REPAIR.11.9`'s rejected gate at 114 of 131). ⛔ The rule is about the ARGUMENT, not the call: both tools name by exclusive creation, which is the half this doctrine wants — a pattern flagging the call itself would condemn 15 conforming sites and teach bypass. ⚠️ Shell and Python skip comments and Rust does not, because every repaired site carries a comment naming the command it explains; both directions are asserted. 🔴 The gate flagged ITSELF on its first run, its diagnostic message spelling the command it matched — the `SELF-TEST` founding incident repeating in a file carrying a `--self-test` of its own — and is fixed by RE-WORDING rather than a path exclusion, so it self-polices. Whole-tree; exceptions verbatim in `.doctrine/storage_locality_exceptions.txt`, and a stale entry is also a breach | `scripts/check_storage_locality.sh` (`--self-test`) |
| `BOOK-LINKS` | every intra-book Markdown link resolves to a file that exists. ⭐ `mdbook build` does not validate links, so three dead ones shipped to the rendered book. Their cause is the instructive part: `DOCPATH` requires repo-root-relative references, an author applied that to INTRA-BOOK navigation, and mdbook resolves a link relative to its page — so `cli.md` rendered `href="docs/book/src/cli-state.html"` while the page is `cli-state.html`. The doctrine's intent was satisfied and the navigation broke. External URLs are deliberately out of scope: a network call in a commit hook is a flake generator | `scripts/check_book_links.sh` (`--self-test`) |
| `REASON-CODE-DOC` | every reason code the SERVER emits is named in the book's error table. ROADMAP §9.8 publishes a stable registry that `KnownReasonCode` mirrors exactly, and the product emits codes that POSTDATE it — `python3 -B scripts/census_reason_codes.py` prints all three populations. 🔴 **This row restated them as bare numerals and they had DRIFTED**, from the commit `SIGNOFF-REPAIR.9.2.1.1` added a code and updated the enforcer's own comment without the mirror. ⛔ **And they became true again BY ACCIDENT**, when `.11.14.3.2` retired a different code and took the count back — so a reader comparing the two would have found them agreeing and concluded nothing had ever drifted. That coincidence is why the numbers are GONE and the command is here, rather than being quietly corrected to today's (`SIGNOFF-REPAIR.11.16`). Nothing broke — `ReasonCode::Unknown` preserves an unregistered code verbatim, which is the designed forward-compatibility path — but nothing told a client author those nine existed, so the only way to learn about `quota_unconfigured` was to receive one. ⛔ Emission is SERVER-side: a `code:` literal in the node or CLI crate is a client building a local error when a body will not parse, and counting those inflates the emitted population by the client-side sentinels, which the census reports on their own line. ⭐ The gate fires on ZERO breaches today and would have fired on all nine, which is the shape a gate should have — it catches the NEXT drift instead of presenting a backlog (contrast `SIGNOFF-REPAIR.11.9`, where the same rule shape would have fired on 114 of 131 and was rejected) ⭐ **A SECOND ARM, `ACQUISITION-KIND-DOC`, RIDES THE SAME INVOCATION** (`SIGNOFF-REPAIR.7.2.10`). An acquisition refusal travels in `acquisition_error.kind`, NOT in `code`, and the two vocabularies share **not one string** — so this census was RIGHT to key on `code:`, and the gap it left was real: **41** kinds a client branches on, **0** documented. ⭐ Derived from the PRODUCERS — each `AcquisitionError { kind: … }` construction walked from `kind:` to `message:` — because two cheaper keys were wrong at once: a `kind: match &error {` marker MISSED the `GitError` site, written without the `&`, and a loose `kind:` regex also matched `derived_kind:` and `actor_kind:`, sweeping in four strings from unrelated structs. A third, a fixed-length window, overran into a `#[serde(rename_all = …)]` attribute. All three are pinned as arms with their own reds. ⛔ A tree-wide `=> \"…\"` scan returns **93** literals and is a POPULATION, not a vocabulary; it is published as nothing. ⚠️ The set is OPEN at **5** sites forwarding a worker-chosen kind, and the book says so rather than implying closure. ⭐ Calibrated at **0 of 200 commits** introducing a new kind, and `.11.18.2`'s discriminator applies in the unusual direction: the `code` half has long been gated, but the `kind` half was gated by NOTHING, so this zero is real rather than survivorship | `scripts/census_reason_codes.py --check` (`--self-test`) |
| `ATTACH-LANDED` | every `attach` clause in `RECONCILIATION.md` is NAMED by the leaf that owns it. ⭐ `attach` is the ONE state of the ledger's six whose Next action is not "none": it requires a SENTENCE to be written into a leaf the classifier does not own, and every other property of a row — a real record, a state in the closed set, a real owner, no duplicate clause — is visible in the row itself. 🔴 So `SIGNOFF-REPAIR.11.9`'s own mechanism was live INSIDE the instrument built to stop it: tranche 1 classified three clauses `attach` and performed none of the three attachments, and the only reason it was found is that a human read two leaves end to end. ⛔ The rule is the RECORD ID in the OWNER'S OWN SECTION, never a phrase — matching prose would have to guess at paraphrase, which is the failure mode `SIGNOFF-REPAIR.11.6` measures — and it does NOT require the words `ATTACHED CLAUSE`, which are a formatting convention rather than a contract. The record id is matched with a right-hand boundary, because `R-53-4` is a prefix of `R-53-41`. ⭐ Measured at four points before registering: **3 of 3 breaching** at tranche 1's close (`5862837`), then **0 of 7**, **0 of 15** and **0 of 26** at each tranche close since. That is `REASON-CODE-DOC`'s shape — zero today, every historical instance caught — and not the backlog shape `SIGNOFF-REPAIR.11.9` rejected at 114 of 131 | `python3 -B scripts/census_record_reconciliation.py --classified` (`--self-test`) |
| `ACTION-BOUNDARY` | the four facts blocker **B3**'s deferral rests on are unchanged: `claude.rs::EXEC_ARGS` passes `--restricted --tools ''`, `codex.rs::EXEC_ARGS` passes `--sandbox read-only`, `threads::work_payload` dispatches no acquired bytes, and every adapter declares `tool_support: false` across 4 `Adapter` implementers. ⭐ It exists because a deferral's revisit trigger was PROSE — Phase 1's fuzz deferral fired at Phase 4 and nothing noticed (`SIGNOFF-REPAIR.11.4.7.2`). ⛔ It does NOT prove the boundary holds, only that the argument's premises are unchanged; and the ladder that would enforce this at runtime has no caller (`.13.1.1`) | `scripts/check_action_boundary.sh` |
| `SECRET-SCAN` | `gitleaks detect` finds no secret in the working tree or in history. ⭐ Wired at COMMIT time because a commit CAN introduce a secret — it is change-triggered, unlike the dependency gate below. ⛔ **TWO ARMS:** `--staged` first (0.03 s, reads what is actually being committed), then history. The staged arm exists because the history arm ALONE shipped a defect — a history scan in a pre-commit hook validates the PREVIOUS state, so REPAIR-0200 verified itself green against an uncommitted tree and its own new value surfaced one commit later (`SIGNOFF-REPAIR.13.1.2`). ⚠️ `detect --no-git` was measured and rejected: it walks `target/` and did not finish in 120 s. A verified test literal is cleared by its EXACT historical fingerprint in `.gitleaksignore`, never by suppressing the path or the rule; a rename cannot clear a historical finding, because `detect` scans every commit. ⚠️ SKIPS LOUDLY when `gitleaks` is absent rather than failing closed, and says so on stderr — a skip is a weaker guarantee than a pass. 1.07–1.15 s over 488 commits (`SIGNOFF-REPAIR.11.4.7.2.3`) | `scripts/check_doctrines.project.sh` |
| `PROJECT-SPECIFIC` | this project's own doctrines | `scripts/check_doctrines.project.sh` |

**Project-specific doctrines go in `scripts/check_doctrines.project.sh`** (the pluggable
slot) — never in the universal driver. That is where a project adds the equivalent of its
own build gates, format checks, invariant proofs, etc.

## Adding a doctrine

1. Write `scripts/check_<name>.sh` — cheap, deterministic, self-describing; exit nonzero
   with a one-line stderr message on breach. Keep it fast (heavy proofs belong in CI).
2. Register it — universal → the `DOCTRINES` array in the driver; project → append it to
   `scripts/check_doctrines.project.sh`. ⛔ **Write the registry description in plain prose:**
   each entry is a bash double-quoted string, so a backtick or `$(…)` there is command
   substitution that the driver EXECUTES on every commit and in CI — measured once, it
   printed `pending: command not found` and rendered the description with those words
   silently missing. A self-guard at the top of the driver now refuses it before the
   array is assigned. The backticked spelling belongs in the table above, which is Markdown.
3. Mirror it in the table above (this file is the human-readable mirror of the registry).

## The task-acceptance checklist (every code-change leaf must pass)

A task-tree owner is a direct `docs/tasks/<TREE-ID>.md` file, as defined in
`docs/TASK_TREE_README.md`. Nested evidence files may accompany that owner; they
neither need ownership checklists of their own nor substitute for the real tree.
The blank `TEMPLATE.md` remains excluded.

A code change cannot commit until its owning task-tree leaf records all six:

- [ ] **REPRODUCE / ISSUE** — the problem, shown (not asserted).
- [ ] **ROOT CAUSE (WHY + WHERE)** — tool-backed and pinpointed (`TOOLBOX.md`).
- [ ] **FIX** — the change, made at the lowest-risk level that actually works.
- [ ] **ADDRESSED (verified)** — measured before→after (the global metric where one exists).
- [ ] **NO REGRESSION** — the guard set stays green; state how you proved it.
- [ ] **LOCKSTEP** — live docs (`MEMORY.md`, `CHANGELOG.md`, `DEV_NOTES.md`,
  `LIVE_STATUS.md`), the book, and any trackers updated in the SAME commit.
