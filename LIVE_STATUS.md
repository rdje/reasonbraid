# LIVE_STATUS.md — authoritative live progress tracker

Rows use ONLY these four states: **Done · Mostly Done · In Progress · Not Started**.
Review and update before every commit whenever actual closure or remaining scope changes;
summarize the snapshot in every commit-workflow completion message.

| Area | Status | Notes |
| --- | --- | --- |
| Discipline spine | Done | memory architecture · task-trees · commit workflow · doctrine enforcement · mdBook skeleton |
| Roadmap pair landed (`ROADMAP.md` + companion `KICKOFF.md`) | Done | `RB-SEED.1`; v0.4.1 frozen; Phase 0 companion is `KICKOFF.md` |
| Roadmap seeded into task-trees | Done | `PROGRAM` + `PHASE-0`…`PHASE-9`; census in `RB-SEED.2` |
| Claim-verification architecture | Done | `docs/CLAIM_VERIFICATION.md`; `RB-SEED.3` |
| Phase 0 — contracts and kill-risk experiments | Done | `.0.1`–`.0.8` + `.1.1`–`.1.4` done (WP1 complete); `.2.1` atomic transaction + `.2.2` leased outbox worker done (**WP2 complete**); `.3.1` node journal + inspection CLI + `.3.2` outbound node channel done (**WP3 complete**); `.4.1` fake adapter + supervisor + `.4.2` first real harness (Codex CLI) done (**WP4 complete**); `.5.1` authority engine + `.5.2` budget engine done (**WP5 complete**); `.6.1` control API + CLI + `.6.2` node wiring + two-host demo done (**WP6 complete**); `.7` deliberation benchmark done (**WP7 complete**); `.8.1` gate package + `.8.2` exit-gate closure (**WP8 complete** — ADR-002 **signed by the accountable owner 2026-09-06**: Phase 0 formally exits, KICKOFF §7); `MAINT-1` (README_POLICY re-adopted) + `MAINT-2` (ReasonBraid-only naming sweep) done — **the PHASE-0 tree is complete** |
| Phase 1 — trustworthy LAN vertical slice | Done | opened by the ADR-002 signature; `.1` complete (`.1.1.1`–`.1.1.3`); **`.1.2` complete** — `.1.2.1` node enrollment, `.1.2.2` authenticated channel + lease/presence, `.1.2.3` inbox retention + quarantine (backlogs 11–14); **`.1.3` complete** — `.1.3.1` explicit-participants contract (pending invitations, accept/decline/remove, derived expiry, dispatch-on-accept) + `.1.3.2` simple subscriptions (`thread.join`, invite/join-door enforcement); `MAINT-1` done (ephemeral PG test data re-derived to `$ROOT/target/` — §13); **`.1.4` complete** — `.1.4.1` Claude CLI adapter core (the `.4.2` mirror, wire pinned by live probes) + `.1.4.2` env-gated live qualification (first-run pass on the real 2.1.263 harness, money cost, ledger + book + decision record) — backlogs 19–21 done; **`.1.5` complete** — `.1.5.1` typed §8.5 contribution kinds + evidence references, `.1.5.2` server-assigned rounds + the `thread_advance_round` grant, `.1.5.3` the honest close (core `Inconclusive` terminal + the unresolved register) — backlog 17 done; `MAINT-2` done (the stderr-drain race reproduced + fixed in both adapters); **`.1.6` complete** — `.1.6.1` budget read surface + `.1.6.2` the embedded static shell at `/` (read-only, text-safe, contract-tested) + `.1.6.3` the demo's console beat (backlog 18 done); `MAINT-3` done (the toolchain pin: `rust-toolchain.toml` + CI pin `1.98.0`, tree normalized once under it); **`.1.7` complete** — `.1.7.1` `make dev` (the one-command dev loop + `--check` beat) + `.1.7.2` release packaging (`make release`, `deploy/` runbook, the book's `deployment` chapter, the release-built demo proof); **`.1.8` complete** — `.1.8.1` the audit-reconstruction demo leg (30/30) + `.1.8.2` the G1–G2 gate package (gate record **Met** with five named deferrals, the §19.8 subtraction record, the Phase-1 evidence manifest) — **Phase 1 is COMPLETE** (Demonstration A passed 30/30, debug + release-built); the frontier moved to `PHASE-2.1` |
| Phase 2 — delivery, identity, and recovery hardening | Done | opened by the Phase-1 G2 close; `.1` decomposed at the census seams (six tool-backed gaps: no cert machinery, no revocation write path, no delegation context, no decision cache, no incarnation/run writers, no ledger issuer row); **`.1.1` done** — the ADR-006/007 spike: the project-local CA model measured (TLS 1.3 client-cert verdicts 6/6, issuance p50 63 µs/p95 69 µs), ADR-006 + ADR-007 accepted, the ledger identity row filled; `.1.2` split at the issuance-vs-channel seam; **`.1.2.1` done** — cert issuance at enrollment (migration 0011 `server_ca` + `node_certificates`; the persisted CA survives the rebuild test; the enroll response carries the leaf + dev-escrowed key; `rb-node` stores `cert.der`/`key.der`; the HMAC channel untouched — the coherent interim); **`.1.2.2` done** — the channel v3 cert-proof handshake (chain + validity + fingerprint + signature before any ledger read) + the additive rotate endpoint + the node's ≤50%-lifetime rotation (19 channel tests + the demo 31/31; the ring-SPKI interop discovery recorded in `docs/decisions/2026-09-07_cert-proof-verification.md`); `.1.3` split at the cert-vs-grant seam (the refusal paths exist — the handshake checks `revoked_at`, the evaluation filters `status = 'active'` — the WRITE paths don't); **`.1.3.1` done** — node/cert revocation + the suspended presence + `rb node revoke` + the demo beat; **`.1.3.2` done** — the grant/boundary revoke verbs + the admin inspection lists + the freeze carve-out (reads survive the boundary revocation); `.1.4` split at the ADR-vs-implementation seam; **`.1.4.1` done** — ADR-009 accepted (chain-in-envelope) + the pure `DelegationConstraints`/`delegation_scope_is_subset` prototype; **`.1.4.2` done** — the delegation implementation (the envelope's `authority_context`, the dual evaluation, the scope ladder, the CLI flags); `.1.5` split at the ADR-vs-implementation seam (the census: no cache machinery + no revocation epoch, but the journal's `authz_ref` is pre-shaped); **`.1.5.1` done** — ADR-008 accepted (the shipped in-tx evaluator stays; the node caches ONLY the admission decisions riding its delivery) + the pure cache semantics (44 core tests; the verification caught + fixed the `.1.4.2` schema-golden drift — recorded in `docs/decisions/2026-09-07_verification-set-coverage.md`); **`.1.5.2` done** — the cached-decision machinery: migration 0013 (the tenant revocation epoch + the inbox's decision columns), the `.1.3` revocation writes bump the epoch in-transaction, the delivery carries the admission decision (authz_ref + digest + decided_at + epoch-at-decision; CHANNEL_VERSION 4), the node journals the cached decision and the dispatch boundary refuses expired/epoch-stale/denied/absent ones (journaled, adapter never invoked); the measured live leg proves a revocation makes the NEXT dispatch refuse without a re-ask; `.1.6` split at the incarnation-vs-run seam (the census: the 0007 hierarchy is schema-only — deferral #4; the enroll request carries no §8.1 facts); **`.1.6.1` done** — the incarnation writer (the enroll request's §8.1 facts → the `incarnations` row for role nodes + `GET /v1/admin/incarnations` + `rb inspect incarnations` + `rb-node`'s four flags + the demo beat, 33 checks); **`.1.6.2` done** — the run writer (migration 0014 + the result fold's run row after the idempotency claim — one result = one run — + `GET /v1/admin/runs` + `rb inspect runs` + the demo beat, 34 checks); **`.1.6` complete — the Phase-1 deferral #4 closes — `.1` is COMPLETE** (all six census gaps closed); `.2` split at the contract seams (the census: the `.1.2.2` lease/fencing + the `.1.2.3` quarantine exist in their Phase-1 forms; retry policy, dead-letter/replay, ADR-005 open); **`.2.1` done** — ADR-005 accepted (the PostgreSQL queue: the WP2 leased outbox worker + ADR-004/006 promote; the broker trigger named; no code changes); **`.2.2` done** — the lease epoch (migration 0015 + CHANNEL_VERSION 5: every fenced write carries the epoch it saw, a stale-epoch renewal matches no row, the events transaction re-verifies the pair FOR UPDATE — node_channel 22); **`.2.3` done** — the retry policy (the pure §14.6 `retry_decision` — a budget denial is terminal, a reserved pre-dispatch refusal retries bounded, an ambiguous outcome needs the explicit `allow_possible_duplicate` flag — + the worker's retry gate; core 49 + four worker legs); **`.2.4` done** — the two-way quarantine (the once-only dead-letter report, the server's auto-quarantine in the receipt transaction, `POST /v1/nodes/replay` + `rb node replay`, the decision-scoped retry re-arm — node_work 8); **`.2` is COMPLETE**; `.3` split at the contract seams (the census: the state machine + the settlement + the ambiguity basics exist — circuit breakers, the reconciliation surface, ADR-012/013 are open); **`.3.1` done** — ADR-012 + ADR-013 accepted (the shipped ambiguity contract + the WP5 budget invariants promote — no code; the pricing-snapshot trigger named); **`.3.2` done** — the spend circuit breakers (migration 0016's per-tenant latch: a crossing trips IN the reservation transaction, a tripped breaker refuses everything new, the arm/reset/inspect verbs + `rb breaker arm|reset` + `rb inspect breakers` — budget 9); **`.3.3` done** — the usage-reconciliation surface (`GET /v1/admin/usage` + `rb inspect usage`: held/settled/overrun/denied summed over the ledger rows, measured — command_api 17); **`.3` is COMPLETE**; `.4` split at the contract seams (the census: no backup/restore tooling, the upgrade path unexercised, the inventory has nothing to bind — named deferrals); **`.4.1` done** — the backup + restore automation (`scripts/backup.sh`/`restore.sh` + the guard's restore exercise: seed → real pg_dump → mutate → restore into an isolated database → assert the pre-mutation state — the backup is restored on every guard run, 14 suites); **`.4.2` done** — the migration upgrade test (the guard's `migration_upgrade` suite: all-but-last migrations + the real-API seed + the upgrade + the survival assertions — 15 suites); **`.4.3` done** — the inventory-groundwork deferral record (the absent §17.5/§17.6 controls named with their triggers — no placeholder infrastructure); **`.4` is COMPLETE**; `.5` split at the contract seams (the census: 16 `eprintln!` sites, no metrics/traces/SLO/runbook — the four-record doctrine is structurally true but nothing measures); **`.5.1` done** — ADR-023 accepted (the four records are separate systems, the §18.2 redaction rules pin the future sink, the OpenTelemetry dependency waits for the trigger — no code); **`.5.2` done** — the structured-log + metrics slice (the process-wide registry's seven counters on the real refusal paths + the `log_event!` JSON lines + `GET /v1/admin/metrics` with the caller-HOLDS-tenant_admin gate + the measured test: the denial delta matches the denied record for the actor handle — command_api 18, 15 suites + demo 34/34); **`.5.3` done** — the SLO record (`docs/decisions/2026-09-07_phase2-slo-hypotheses.md`: the guard IS the dev profile's population — SLO-1…SLO-4 target 100 % with a zero error budget, SLO-5 is the measured latency baseline, every unmeasured family named with its trigger) + the node lost/replaced runbook (`docs/runbooks/node-lost-replaced.md`, the full §18.6 shape; closure tests name the existing exercises); **`.5` is COMPLETE**; `.6` split at the contract seams (the census: the capability/unsupported/ambiguity/usage-confidence surface + the credential-scanned corpus exist; the harness is four separate files, the corpus is unpinned as a replay oracle, five §19.4 items have no machinery — named); **`.6.1` done** — the adapter conformance harness (one suite, three adapters, the six §19.4 invariant checks; the scenario shape is the registration surface; the shared provider stubs); **`.6.2` done** — the permanent failure-fixture corpus (the versioned manifest + the exact-match drift check + the §19.4-item coverage check: the oracle can only change additively with a recorded reason); **`.6.3` done** — the qualification checklist (the book's six-box gate) + the deferrals record (the five absent-surface §19.4 items, each with its trigger); **`.6` is COMPLETE**; `.7` split at the census seams (the census: the non-escalation foundations exist but no adversarial suite names §16.12's line; the replacement drill is the runbook's named gap; the retry-safety property has four measured legs; ADR-022 unopened); **`.7.1` done** — the non-escalation property suite (4 measured adversarial tests: cross-tenant 403/404/409, the confused-deputy 403 despite its own grant, the re-arm fence at enrollment, the revocation fence around history; the guard grew to 16 live suites); **`.7.2` done** — the node-replacement drill (the measured ritual: `outcome_unknown` → journal destroyed → revoke → replacement enroll → inbox replay → the epoch-stale refusal fence → dead letter → operator replay → exactly one fold; the replacement enroll path + migrations 0017/0018; the runbook carries the ritual; the guard grew to 17 live suites); **`.7.3` done** — ADR-022 accepted (the shipped audit linkage is the chain's groundwork; the chain + checkpoint + verification policy wait for the named trigger) + the retry-safety inventory (six measured legs: the machine refuses every silent re-dispatch by default); **`.7.4` done** — the Phase-2 subtraction record (S-1…S-13) + the G6–G7 feed (every gate line mapped to evidence or OPEN); **`.7` COMPLETE — Phase 2 is CLOSED** |
| Phase 3 — directory, unknown membership, and autonomous initiation | Done | opened by the Phase-2 close; `.1` decomposed at the census seams (the §10.1 profile is a greenfield); **`.1.1` done** — ADR-014 accepted (deterministic structural eligibility first; the embedding engine behind its trigger); **`.1.2` done** — the profile schema + the write surface (migration 0019, the typed §10.1 fields, the content-addressed version history, the write + attest verbs, the measured suite; the guard grew to 18 live suites); **`.1.3` done** — the per-reader visibility enforcement (the four-reader measurement, the absent-not-nulled filter, the self-describing class, the full-only history); **`.1` is COMPLETE**; `.2` split at the census seams (the shipped lease/presence forms vs the three gaps); **`.2.1` done** — the presence state machine (the six states derived with the honesty precedence; the response names the state; `busy` is the named `.4` trigger); **`.2.2` done** — the offline-known distinction + the operator's enumeration (the expired-lease node reads offline with its expiry visible; the unknown id stays the typed 404; the stale handling pinned as the third leg); **`.2.3` done** — the privacy-filtered directory views (the three measured scopes: the owner's full own view, the member's tenant-filtered view, the network pseudonyms; the zero-visibility rule); **`.2` is COMPLETE**; `.3` split at the census seams (the inputs exist; the expression/evaluator/scoring/surface are the gaps); **`.3.1` done** — the eligibility expression + the pure visibility-scoped stage-1 evaluation (six measured gates, the named reasons); **`.3.2` done** — the stage-2 explainable ranking (the five weighted features with the visibility-safe explanations, the eligible-only ordering, the deterministic tie-break); **`.3.3` done** — the matching query surface (the scope clamp, the reader-filtered candidates, the provenance gate: a role's own write is self-asserted-only); **`.3` is COMPLETE**; `.4` split at the census seams (the baseline + the candidates exist; the vocabulary/call/storm machinery are the gaps); **`.4.1` done** — ADR-015 accepted (the explicit invitation promotes as the baseline; the open call consumes the matching lane; the dependence indicators land with `.6`); **`.4.2` done** — the call artifact + the typed responses (migration 0020, the four verbs, the participation-only eligibility gate, the ranked panel with the explanation; the profiles suite grew to 8); **`.4.3` done** — the storm controls (the fan-out caps + the expiry binding built + measured; the six §10.7 deferrals named with their triggers); **`.4` is COMPLETE**; `.5` split at the census seams (the inbox machinery exists; the ladder/wake/auto-initiation are the gaps); **`.5.1` done** — the delivery-state machine (migration 0021's derived view; the inspection shows the state per row; node_channel 24); **`.5.2` done** — the subscriptions + the wake gate (migration 0022's server-recorded offers + the delivery-boundary zero-concurrency hold; profiles 10, node_channel 25); **`.5.3` done** — the node-initiated thread API (the explicit `thread_create_auto` grant + the server-side §11.5 checklist with the typed refusals; profiles 11); **`.5` is COMPLETE**; `.6` split at the census seams (the lineage inputs exist; the computation/feature/labels are the gaps); **`.6.1` done** — the dependence-indicator computation (the five attributes, the named overlap groups, the no-overclaim explanations; five unit tests); **`.6.2` done** — the diversity feature + the panel wiring (the ranking's sixth feature + the snapshot's dependence indicators; matching 12, profiles 12); **`.6.3` done** — the label discipline sweep (every hit is the roadmap's rule or a negation — no surface claims the forbidden term); **`.6` COMPLETE — Phase 3 is CLOSED** |
| Phase 4 — universal resource and evidence pipeline | In Progress | opened by the Phase-2/3 closes; `.1` decomposed at the census seams (the resource surface is a greenfield: the reason name + the typed `EvidenceRef`s exist, the contract/registry/ADRs do not); **`.1.1` done** — ADR-011 + ADR-018 accepted (the `sha256:<hex>` digest scheme + the isolation-class vocabulary); **`.1.2` done** — the typed `ResourceReference` + the submission (migration 0023, the replay/conflict immutability, the typed 422/400 refusals; profiles 13); **`.1.3` done** — the resolver capability registry (the §12.2 advertise + the filter-then-rank resolution + the explicit unresolvable-now; profiles 14); **`.1` is COMPLETE**; `.2` split at the census seams (nothing fetches — the classification/fetcher/receipt are the greenfield); **`.2.1` done** — the destination classification + the SSRF policy (the pure §12.4 rules, the public-only evaluation, the 18-case refusal matrix); **`.2.2` done** — the safe HTTPS fetcher (the hardened parse, the ceilings, the per-hop redirect policy, the two-layer destination enforcement, the manual decode + the ratio brake — 16 OFFLINE tests, the SSRF proof measured; the sweep also fixed the workspace rustls-provider ambiguity + ROUTED 9 pre-existing clippy findings); **`PHASE-4-MAINT-1` done** — the clippy evidence debt repaired (`cargo clippy --all --all-targets -- -D warnings` → rc=0, twelve findings); **`.2.3` done** — the snapshot receipt + the R0 pack wiring (the built-in executes through the resolve path; profiles 15) — **`.2` COMPLETE (pack R0)**; `.3` split at the census seams (nothing fetches Git — `.3.1` the contract + the library census → `.3.2` the acquisition → `.3.3` the receipt + the wiring); **`.3.1` done** — the R1 contract + the library census (gix over the classified transport; the decision record); **`.3.2` done** — the R1 acquisition (gix behind the classified dial — the budgets + the named refusals + the resolved commit; five tests); **`.3.3` done** — the R1 receipt + the pack wiring (the git references resolve to the built-in; profiles 16) — **`.3` COMPLETE (pack R1)**; `.4` split at the census seams (nothing extracts — `.4.1` the contract + the parser census → `.4.2` the workers → `.4.3` the receipt + the wiring); **`.4.1` done** — the R2 contract + the parser census (the Derivation-only extraction, the worker quarantine, the decision record); **`.4.2` done** — the extraction worker (`crates/reasonbraid-extract` — the stdio quarantine, nine tests); **`.4.3` done** — the R2 receipt + the pack wiring (the hinted references pipeline acquire→extract; profiles 17) — **`.4` COMPLETE (pack R2)**; `.5` split at the census seams (nothing exists for the browser/broker/agent-mediated acquisition — `.5.1` the contracts + the opt-in gate → `.5.2` the machinery → `.5.3` the receipt + the wiring); **`.5.1` done** — the three contracts + the OPT-IN gate (the disclosed broker, the deployment-checked browser, the §12.8 vocabulary — the decision record); **`.5.2` done** — the machinery (the browser worker with the REAL-Chrome tests, the broker, the §12.8 vocabulary — compiled, unwired); **`.5.3` done** — the gated receipt + the wiring (the startup sync — the disabled pack has no row; profiles 18) — **`.5` COMPLETE (the gated lane)**; `.6` split at the census seams (the receipts exist, nothing persists them — `.6.1` the store + the tombstone → `.6.2` the graph → `.6.3` the claims → `.6.4` the retention); **`.6.1` done** — the snapshot store + the tombstone (the verified content-addressing, the R0/R2/R5 auto-submits; profiles 19); **`.6.2` done** — the derivation graph (the verified edges + the traversal; profiles 20); **`.6.3` done** — the claim-evidence graph + the citation validation (the excerpt-in-the-bytes check; profiles 21); **`.6.4` done** — the license/retention + the freshness (the TTL enforcement + the staleness; profiles 22) — **`.6` COMPLETE (the evidence pipeline)**; frontier **`.7`** — the G4 hostile-content suite (the phase's exit gate) |
