# LIVE_STATUS.md — authoritative live progress tracker

Rows use ONLY these four states: **Done · Mostly Done · In Progress · Not Started**.
Review and update before every commit whenever actual closure or remaining scope changes;
summarize the snapshot in every commit-workflow completion message.

| Area | Status | Notes |
| --- | --- | --- |
| Discipline spine | Done | memory architecture · task-trees · commit workflow · doctrine enforcement · mdBook skeleton |
| Roadmap pair landed (`ROADMAP.md` + companion `KICKOFF.md`) | Done | `RB-SEED.1`; v0.4.1 frozen; Phase 0 companion is `KICKOFF.md` |
| Roadmap seeded into task-trees | Done | `PROGRAM` + `PHASE-0`…`PHASE-9`; census in `RB-SEED.2` |
| Claim-verification architecture | Done | `docs/CLAIM_VERIFICATION.md`; `RB-SEED.3` |
| Phase 0 — contracts and kill-risk experiments | Done | `.0.1`–`.0.8` + `.1.1`–`.1.4` done (WP1 complete); `.2.1` atomic transaction + `.2.2` leased outbox worker done (**WP2 complete**); `.3.1` node journal + inspection CLI + `.3.2` outbound node channel done (**WP3 complete**); `.4.1` fake adapter + supervisor + `.4.2` first real harness (Codex CLI) done (**WP4 complete**); `.5.1` authority engine + `.5.2` budget engine done (**WP5 complete**); `.6.1` control API + CLI + `.6.2` node wiring + two-host demo done (**WP6 complete**); `.7` deliberation benchmark done (**WP7 complete**); `.8.1` gate package + `.8.2` exit-gate closure (**WP8 complete** — ADR-002 **signed by the accountable owner 2026-09-06**: Phase 0 formally exits, KICKOFF §7); `MAINT-1` (README_POLICY re-adopted) + `MAINT-2` (ReasonBraid-only naming sweep) done — **the PHASE-0 tree is complete** |
| Phase 1 — trustworthy LAN vertical slice | Done | opened by the ADR-002 signature; `.1` complete (`.1.1.1`–`.1.1.3`); **`.1.2` complete** — `.1.2.1` node enrollment, `.1.2.2` authenticated channel + lease/presence, `.1.2.3` inbox retention + quarantine (backlogs 11–14); **`.1.3` complete** — `.1.3.1` explicit-participants contract (pending invitations, accept/decline/remove, derived expiry, dispatch-on-accept) + `.1.3.2` simple subscriptions (`thread.join`, invite/join-door enforcement); `MAINT-1` done (ephemeral PG test data re-derived to `$ROOT/target/` — §13); **`.1.4` complete** — `.1.4.1` Claude CLI adapter core (the `.4.2` mirror, wire pinned by live probes) + `.1.4.2` env-gated live qualification (first-run pass on the real 2.1.263 harness, money cost, ledger + book + decision record) — backlogs 19–21 done; **`.1.5` complete** — `.1.5.1` typed §8.5 contribution kinds + evidence references, `.1.5.2` server-assigned rounds + the `thread_advance_round` grant, `.1.5.3` the honest close (core `Inconclusive` terminal + the unresolved register) — backlog 17 done; `MAINT-2` done (the stderr-drain race reproduced + fixed in both adapters); **`.1.6` complete** — `.1.6.1` budget read surface + `.1.6.2` the embedded static shell at `/` (read-only, text-safe, contract-tested) + `.1.6.3` the demo's console beat (backlog 18 done); `MAINT-3` done (the toolchain pin: `rust-toolchain.toml` + CI pin `1.98.0`, tree normalized once under it); **`.1.7` complete** — `.1.7.1` `make dev` (the one-command dev loop + `--check` beat) + `.1.7.2` release packaging (`make release`, `deploy/` runbook, the book's `deployment` chapter, the release-built demo proof); **`.1.8` complete** — `.1.8.1` the audit-reconstruction demo leg (30/30) + `.1.8.2` the G1–G2 gate package (gate record **Met** with five named deferrals, the §19.8 subtraction record, the Phase-1 evidence manifest) — **Phase 1 is COMPLETE** (Demonstration A passed 30/30, debug + release-built); the frontier moved to `PHASE-2.1` |
| Phase 2 — delivery, identity, and recovery hardening | In Progress | the `PHASE-2` tree is `active` (unblocked by the Phase-1 G2 close); `.1` decomposed at the census seams (six tool-backed gaps: no cert machinery, no revocation write path, no delegation context, no decision cache, no incarnation/run writers, no ledger issuer row); **`.1.1` done** — the ADR-006/007 spike: the project-local CA model measured (TLS 1.3 client-cert verdicts 6/6, issuance p50 63 µs/p95 69 µs), ADR-006 + ADR-007 accepted, the ledger identity row filled; `.1.2` split at the issuance-vs-channel seam; **`.1.2.1` done** — cert issuance at enrollment (migration 0011 `server_ca` + `node_certificates`; the persisted CA survives the rebuild test; the enroll response carries the leaf + dev-escrowed key; `rb-node` stores `cert.der`/`key.der`; the HMAC channel untouched — the coherent interim); **`.1.2.2` done** — the channel v3 cert-proof handshake (chain + validity + fingerprint + signature before any ledger read) + the additive rotate endpoint + the node's ≤50%-lifetime rotation (19 channel tests + the demo 31/31; the ring-SPKI interop discovery recorded in `docs/decisions/2026-09-07_cert-proof-verification.md`); `.1.3` split at the cert-vs-grant seam (the refusal paths exist — the handshake checks `revoked_at`, the evaluation filters `status = 'active'` — the WRITE paths don't); **`.1.3.1` done** — node/cert revocation + the suspended presence + `rb node revoke` + the demo beat; **`.1.3.2` done** — the grant/boundary revoke verbs + the admin inspection lists + the freeze carve-out (reads survive the boundary revocation); `.1.4` split at the ADR-vs-implementation seam; **`.1.4.1` done** — ADR-009 accepted (chain-in-envelope) + the pure `DelegationConstraints`/`delegation_scope_is_subset` prototype; **`.1.4.2` done** — the delegation implementation (the envelope's `authority_context`, the dual evaluation, the scope ladder, the CLI flags); `.1.5` split at the ADR-vs-implementation seam (the census: no cache machinery + no revocation epoch, but the journal's `authz_ref` is pre-shaped); **`.1.5.1` done** — ADR-008 accepted (the shipped in-tx evaluator stays; the node caches ONLY the admission decisions riding its delivery) + the pure cache semantics (44 core tests; the verification caught + fixed the `.1.4.2` schema-golden drift — recorded in `docs/decisions/2026-09-07_verification-set-coverage.md`); **`.1.5.2` done** — the cached-decision machinery: migration 0013 (the tenant revocation epoch + the inbox's decision columns), the `.1.3` revocation writes bump the epoch in-transaction, the delivery carries the admission decision (authz_ref + digest + decided_at + epoch-at-decision; CHANNEL_VERSION 4), the node journals the cached decision and the dispatch boundary refuses expired/epoch-stale/denied/absent ones (journaled, adapter never invoked); the measured live leg proves a revocation makes the NEXT dispatch refuse without a re-ask; `.1.6` split at the incarnation-vs-run seam (the census: the 0007 hierarchy is schema-only — deferral #4; the enroll request carries no §8.1 facts); **`.1.6.1` done** — the incarnation writer (the enroll request's §8.1 facts → the `incarnations` row for role nodes + `GET /v1/admin/incarnations` + `rb inspect incarnations` + `rb-node`'s four flags + the demo beat, 33 checks); **`.1.6.2` done** — the run writer (migration 0014 + the result fold's run row after the idempotency claim — one result = one run — + `GET /v1/admin/runs` + `rb inspect runs` + the demo beat, 34 checks); **`.1.6` complete — the Phase-1 deferral #4 closes — `.1` is COMPLETE** (all six census gaps closed); `.2` split at the contract seams (the census: the `.1.2.2` lease/fencing + the `.1.2.3` quarantine exist in their Phase-1 forms; retry policy, dead-letter/replay, ADR-005 open); **`.2.1` done** — ADR-005 accepted (the PostgreSQL queue: the WP2 leased outbox worker + ADR-004/006 promote; the broker trigger named; no code changes); **`.2.2` done** — the lease epoch (migration 0015 + CHANNEL_VERSION 5: every fenced write carries the epoch it saw, a stale-epoch renewal matches no row, the events transaction re-verifies the pair FOR UPDATE — node_channel 22); **`.2.3` done** — the retry policy (the pure §14.6 `retry_decision` — a budget denial is terminal, a reserved pre-dispatch refusal retries bounded, an ambiguous outcome needs the explicit `allow_possible_duplicate` flag — + the worker's retry gate; core 49 + four worker legs); **`.2.4` done** — the two-way quarantine (the once-only dead-letter report, the server's auto-quarantine in the receipt transaction, `POST /v1/nodes/replay` + `rb node replay`, the decision-scoped retry re-arm — node_work 8); **`.2` is COMPLETE**; `.3` split at the contract seams (the census: the state machine + the settlement + the ambiguity basics exist — circuit breakers, the reconciliation surface, ADR-012/013 are open); **`.3.1` done** — ADR-012 + ADR-013 accepted (the shipped ambiguity contract + the WP5 budget invariants promote — no code; the pricing-snapshot trigger named); **`.3.2` done** — the spend circuit breakers (migration 0016's per-tenant latch: a crossing trips IN the reservation transaction, a tripped breaker refuses everything new, the arm/reset/inspect verbs + `rb breaker arm|reset` + `rb inspect breakers` — budget 9); **`.3.3` done** — the usage-reconciliation surface (`GET /v1/admin/usage` + `rb inspect usage`: held/settled/overrun/denied summed over the ledger rows, measured — command_api 17); **`.3` is COMPLETE**; frontier **`.4`** — backup/PITR + migrations |
