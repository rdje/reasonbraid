# MEMORY — resume pointer (layer A; overwrite-only, keep ≤ ~50 lines)

## How to resume

- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- Work is tracked in task-trees under `docs/tasks/`; follow `COMMIT.md`.
- Durable facts/decisions live in `docs/decisions/` (+ its `INDEX.md`).
- Transferable methods: `TOOLBOX.md` and `docs/knowledge/` — consult, do not re-derive.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: `REASONBRAID-REPAIR-0288` (leaf `SIGNOFF-REPAIR.6.2.1`): the dedup window kept the OLDEST 64 and now keeps the newest — the repair is a DIRECTION, three lines. ⛔ **A TWO-ELEMENT FIXTURE CANNOT TEST A SIXTY-FOUR-ELEMENT BOUND**: the old control never crossed the boundary, so `push`+`truncate` looked correct. `.6.2`'s census found **three** live defects in that lane (`.6.2`), of which this is the first repaired; ⚪ the first-delivery race did NOT reproduce and is recorded as such, never promoted. ⛔ **A FALSIFICATION THAT SILENTLY DID NOT APPLY IS A GREEN RUN WEARING A RED LABEL** (`.6.1.3`): `cargo fmt` reflowed a guard, the patch anchor stopped matching, the suite passed. Assert the anchor, and `cmp` BEFORE the run as well as after. ⛔ **A NUMBER WHOSE PRODUCER IS NOT TRACKED FAILS LEG 3 WHATEVER ITS TRUTH** (`a-metric-scoped-to-one-record-ages-silently`, `.6.1.5.3.1`'s *subtract*). Ahead count: `git rev-list --count origin/main..HEAD`; push at ~300.
- active_work_unit: `SIGNOFF-REPAIR` → frontier leaf: `SIGNOFF-REPAIR.6.2.2` (`pending`). ✅ CLOSED this session: `.7.1.2.2.1` (0277) · `.6.1.5.2` (0278) · `.7.1.2.2.2` (0279) · `.6.1.5.2.1` (0280) · `.6.1.5.3` (0281) · `.6.1.5.3.1` (0282) · `.6.1.5.4` (0283) · `.7.1.2.2.3` (0284) · `.6.1.3` (0285) · `.6.1.4` (0286). ⭐ **`.6.1` IS FULLY CLOSED** — all five children.
- next_action: `.6.2.2` — the MCP listen cursor is NOT monotonic (recording 100 then 5 leaves 5), and the resume path reads it as the OWN cursor, so one late delivery rewinds the resume point. RED measured at `.6.2`. ⛔ Decide clamp vs refusal WITH A REASON — they differ observably, and the delivery may be legitimate and merely late. Then `.6.2.3` (a malformed window silently emptied), `.6.2.4` (a BUILD, last).
- ⛔ **RLS ALONE IS NOT A GATE HERE, and `.6.1.5.1.1` proved it rather than inferring it** — the dev/test profile connects as SUPERUSER, which bypasses RLS even under `FORCE ROW LEVEL SECURITY`. Always pair `rls::with_tenant_claim` with an explicit `tenant_id` predicate; 3 of the 5 call sites always did. A gate no control can observe is a gate nobody has tested.
- ⭐ **THE `.7.1.2` ADJUDICATION:** the 17 are **4 tenant-owned by derivation · 1 within DOC-0029 · 2 shared journals · 10 shared CONTROL surfaces** — `docs/decisions/2026-09-19_the-policy-registry-is-a-shared-control-surface.md`. ⛔ *A column-less table is not necessarily an ownerless one* is that record's Fact 1. Figures are DERIVED, never carried: `scripts/census_shared_registry_writes.py --check`.
- in_flight_uncommitted: none — working tree clean, no background job owned by this repo (`bash scripts/check_no_background_jobs.sh`). ⛔ **A REFUSAL DETECTED BY LETTING A STATEMENT FAIL DOES NOT SURVIVE BEING MOVED INSIDE A TRANSACTION** (`.6.1.5.4`): PostgreSQL aborts the transaction, so the audit write that follows fails too and a 500 replaces an audited refusal — and a refusal that becomes an audit record may no longer be approximated. Recorded at `policy::register`. ⭐ **A GREEN DOCTRINE GATE IS NOT EVIDENCE A SUITE RUNS** (`.7.1.2.2.1`, 2026-09-19): twenty suites were dead for a migration's worth of commits while `make gate` was green. Before trusting a gate's `--json`, ask which of its derived figures has NEVER MOVED. ⛔ **BUILDS HERE ARE A HOST PROBLEM, not a tree-size one**: a 68m 16s rebuild ran with `syspolicyd` at 44% CPU and swap at 6,151 of 7,168 MB. ✅ `target/debug` was RETIRED (DOC-0060, `.11.4.3.1.8`) — ≈174 GiB — so a cold build is by design. ⛔ `cargo clean` must NOT be used here — it takes cited evidence out of `target/`; the argument is in `a-census-is-as-wide-as-its-key` under *the deletion side*.
- ⛔ **THIS FILE IS A POINTER. A lesson lives in `docs/knowledge/`; this block names it and stops.** Restating one here is the `SIGNOFF-REPAIR.11.16` defect one layer up, and it is what took the file to 6,123 of 7,168 bytes (`.11.20`). Run `python3 -B scripts/census_memory_warnings.py` before evicting anything, and classify every `UNCITED` item by hand against the durable layer — that class is where a FACT, not a pointer, gets lost (`.11.20.1`, `.11.20.3`).
- ⭐ **THE LESSONS ARE A LAYER, NOT A LIST — enumerating them here is the defect bullet 1 names.** 42 notes in `docs/knowledge/`, indexed by the derived `KNOWLEDGE_MAP.md`; methods in `TOOLBOX.md`. The two most often needed: `a-control-that-passes-for-an-unrelated-reason` (in-situ falsification, and why a NEGATIVE arm needs a degenerate implementation) and `calibrate-over-the-history-that-contains-the-instance`.
- blockers: register `SIGNOFF-REPAIR.13` + `docs/book/src/blockers.md`. Column is `Owed here?`. ⛔ **B1, B2, B3 are `yes (deferred)` — director instruction 2026-09-18: Internet exposure is NOT high priority, the LAN must fully work first** (`docs/decisions/2026-09-18_lan-completeness-precedes-internet-exposure.md`). Work is still owed here and `.14` still owns it; it does NOT ride the frontier. ⭐ **The LAN bar is DEFINED: G0–G5 genuinely met + G7 earned on the LAN, G6 out of scope** — G6 is the only gate whose *unlocks* column names a transport. G7's numeric legs (load, SLO) are re-derived on the Internet; its structural legs are earned once. ✅ **C2 is CLOSED** (all five gate records re-derive; `.11.4.7`, REPAIR-0265) — so NO register row is work now; ✅ **G4's one un-discharged strand is DISCHARGED** (`.7.3.6.5`, REPAIR-0270 — the R3 `vm_container` claim corrected to `process`). ⛔ NOT a claim that G4 is Met: the record's *G4 must be re-earned* verdict is unchanged. **B4 `no`** (name clearance). **C1 `no`** — a limit on what may be CLAIMED. ⛔ Deferral changes priority, never claims: Phase 7 G6/G7 stays NOT MET and `.14`'s prohibition stands. A `yes` row rides the frontier; `yes (deferred)` and `no` are surfaced once per session with their trigger.
- ⭐ **DIRECTION, accepted and UNSCHEDULED (2026-09-19): a System One classifier (Jev) is an ENHANCEMENT, not a repair** — the greenlight was conditional on proving it fixes a defect, and the sole candidate was measured and refuted. Blocked by the LAN bar regardless. `docs/decisions/2026-09-19_system-one-classifiers-are-an-enhancement-not-a-repair.md`. No leaf.
- ⚠️ standing corpus rule from `.13.4`: **a correction is not complete until the LIVE-DOCUMENT corpus that restates it has been censused.** Live documents = `README.md`, `LIVE_STATUS.md`, `MEMORY.md`, `docs/book/src/*.md`; the dated ledgers are history and stay.
