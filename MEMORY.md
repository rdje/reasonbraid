# MEMORY — resume pointer (layer A; overwrite-only, keep ≤ ~50 lines)

## How to resume

- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- Work is tracked in task-trees under `docs/tasks/`; follow `COMMIT.md`.
- Durable facts/decisions live in `docs/decisions/` (+ its `INDEX.md`).
- Transferable methods: `TOOLBOX.md` and `docs/knowledge/` — consult, do not re-derive.

## Current state (OVERWRITE this block each update — do not append)

- latest_commit: `REASONBRAID-REPAIR-0237` (leaf `SIGNOFF-REPAIR.11.16`): a restated number needs a PRODUCER, not a rule. Re-derive the ahead-of-origin count with `git rev-list --count origin/main..HEAD`; push at ~300.
- active_work_unit: `SIGNOFF-REPAIR` → frontier leaf: `SIGNOFF-REPAIR.11.19` (`pending`). ✅ CLOSED this session: `.11.14.3` family, `.11.14.1.1`, `.11.14.1.2`, `.11.18`, `.11.17`, `.11.16`.
- next_action: `.11.19` — 🔴 **a blank line ENDS a GFM table, and one sits inside the tree's OWN Current Frontier table**, so its header renders empty and 45 rows render as literal pipe-text on a public repo. ⭐ Widening the key from "after the delimiter row" to "anywhere in the body" took the census **1 → 51 rows across 3 files** (`docs/tasks/SIGNOFF-REPAIR.md` 45, `docs/adr/INDEX.md` 3, `docs/tasks/PHASE-0.md` 3). ⛔ `TABLE-ARITY-RATCHET` governs these files and CANNOT see it — an orphaned row has no header to disagree with, `BOOK-LINKS`' founding shape again. ⚠️ First act is READING the five blanks: one may be a deliberate separator between two tables, where the repair is a second header row, not a deletion. ⭐ Verified by the RENDERER (mdbook: 2 `<table>`, 1 `<tr><td>`, the orphan as `<p>| 1 | 2 |</p>`), not by the spec.
- in_flight_uncommitted: none — working tree clean, no background job.
- ⭐ standing lesson from 2026-09-18 (`.11.16`): **a restated number needs a PRODUCER, not a rule.** Before gating a doc/census mismatch, ask whether the thing that OWNS the number can print it. Three candidate gates priced at 66% / 50% / 71% and all declined (vs `.11.9`'s rejected 87%, `.11.15`'s 93%, `POSITIONAL-REF`'s accepted 9.5%); a per-numeral allowlist refused by `VISIBILITY-POLICY`'s own "thirty entries teaches bypass" at 103. ⭐ The four numbers that HAD gone stale all named a population size the instrument enumerates every run and never printed — two `--census` flags, no gate. ⛔ And the cheap fix was wrong once: a row that drifted and became true again BY ACCIDENT is discharged by DELETING the number, because correcting it teaches the next reader it never drifted. `docs/knowledge/a-restated-number-needs-a-producer.md`.
- ⚠️ standing lesson from 2026-09-17: **a control that passes for an UNRELATED reason** — caught by one question: *what would this have done if the defect were present?* The mechanical form: run the NEW control against the OLD code, in situ; every arm claiming to cover the defect must FAIL by name, and any arm that passes both ways must be LABELLED. Also: a bug's DIRECTION is a claim, not a symmetry (`a-scoping-defect-errs-in-one-direction`); a row keyed on CONTENT may not hold a tenant's DECISION.
- ⚠️ **`a-census-is-as-wide-as-its-key` fired THREE times in one session**, and the third was the worst: the narrow key reported **1** where the wide key reports **51**. Run a census twice, with two different keys, before publishing its number.
- blockers: register `SIGNOFF-REPAIR.13` + `docs/book/src/blockers.md`. Column is `Owed here?`, set from the owning leaf. **B1, B2, B3 `yes`** (work owed HERE — all three share `SIGNOFF-REPAIR.14`'s frozen exposure candidate). **C2 `yes`** (four gate records remain). **B4 `no`** (professional name clearance). **C1 `no`** — not a blocker, a limit on what may be CLAIMED. A `yes` row is WORK and rides the frontier; a `no` row is surfaced once per session with its trigger.
- ⚠️ standing corpus rule from `.13.4`: **a correction is not complete until the LIVE-DOCUMENT corpus that restates it has been censused.** Live documents = `README.md`, `LIVE_STATUS.md`, `MEMORY.md`, `docs/book/src/*.md`; the dated ledgers are history and stay.
