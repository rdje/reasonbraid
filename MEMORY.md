# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public (director correction 2026-09-09 supersedes the wrong README/ADR private instruction). Working-name clearance is open and does not block public visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Local is **121 commits ahead** of `origin/main` (`c17841c`) — re-derive, never carry: `git rev-list --count origin/main..HEAD`. ⚠️ **59 leaves are `pending`** — re-derive with the frontier caption's awk command, which reads BOTH `- Status:` and the `- Opened:`-only spelling (11 carry only the latter).
- **Next action:** row 1 `.3.4.6`; row 1b `.11.4.7`. 🔴 **NO GATE RECORD'S "SHIPPED" COUNT RE-DERIVES** — 5 records, 4 counting lines, all counted BEFORE the source read; G6/G7 line (2) IS tenant isolation and 5 repairs since falsified it. ⛔ Not dishonest — the suites' COVERAGE was never measured. ⚠️ G6/G7's 3 EXTERNAL gaps (outside threat-model review, prompt-injection suite, pen-test) are out of local scope. 🔴 **A GOAL LINE CAN NAME A MECHANISM ITS OWN SPLIT DROPS** (`.3.4`: consent, the hash's TARGET half — `.3.4.6`/`.3.4.7`): COUNT mechanisms against children at every split. ⛔ `dead_code` does NOT see a `pub` item — census callers explicitly. 🔴 **A LEAF WITH NO `- Acceptance:` IS NOT AN OWNER** — `owned`/`attach` measure VISIBILITY, never EXECUTABILITY (`.11.13`). ⛔ **EVERY NUMBER COMES FROM A COMMAND** (`.11.9.1.2.3`/`.11.9.1.3.1`); `git show` at the original commit tells stale from false. Prefer a RATIO, a SINGLE-HIT GREP or a TWO-SITE CONTRAST. ⛔ **ASK THE RENDERER, never the spec** (`knowledge/a-control-is-calibrated-against-the-renderer`); a table pipe needs `\|` even in a code span. 🔴 **A LINE NUMBER into a growing file is `LIVE-DOC-CURRENCY` in another costume — locate by HEADING, or by ENCLOSING FUNCTION** (2 of 8 stale; `.9.2.1.3` mapped a grep hit to the wrong test). ⛔ **`docs/decisions/` BEFORE a mechanism or a decision** (`.9.2.1.1.1`). ⛔ **VERIFY THE PREMISE THE DECISION TURNS ON** (`.9.3.4`; frontier 1d/1e carry the ruling). ⚠️ N probed inputs are a SAMPLE, never the boundary. ⛔ **A NUMBER DECORATING A FINDING GETS NONE OF ITS LEGS** (3 of 3 failed): PIN a census your own change invalidates (`git grep <pat> <sha> --`); ⛔ `*.rs` does NOT recurse; a git PATHSPEC `*` DOES cross `/` — `:(glob)` (`.11.4.5.4`). ⚠️ **SCOPE A CENSUS WHOLE** (`.11.2.2`); ⛔ `git grep -- 'crates/*/src'` silently matches NOTHING. ⛔ **A CITED AUTHORITY MUST BE A HELD ONE** (`.9.3.1`): `authority::grant_held_by`/`grant_is_live` are THE predicate — never a sixth spelling; grant ids are derivable (`grt_<id>`). ⛔ **Two caller-supplied identifiers must be BOUND, not one checked** (`.3.5.3`/`.3.5.4`); `inspect_call` is the shape — tenant DERIVED from the target. ⚠️ A finding names where someone LOOKED — enumerate the core's callers first. ⭐ **Before evicting a warning here, run `scripts/census_memory_warnings.py`** (`.11.4.2.1`: 25 warnings, 0 exist only here — shed one whose line NAMES its leaf). ⭐ **Rank a backlog by its NARROWEST candidate, never by fan-out** (`.11.9.1`). Ledger: `signoff_review/RECONCILIATION.md` (`--rank`/`--classified`). ⛔ **EVERY channel WRITE re-checks the lease IN-TX, row locked** (`.2.2`/`.4.2.3`/`.4.2.4`); ⛔ **`poll` NOT**; ⛔ **no tenant guard on `ack`**. The lease reads the **DB clock** but is WRITTEN process-side, so 60 s TTL is nominal (`.4.2.3.1`). ⛔ **Proof nonces consumed once, AFTER the signature**; `nonce` REQUIRED on handshake/rotate. 🔴 **DELEGATION IS ONE HOP DEEP**: ⛔ do not "fix" `delegable`/`max_delegation_depth`. ⚠️ **Rank a census by REACHABILITY, not resemblance**; "not reachable" usually means *in production*. ⚠️ **Falsify each part SEPARATELY; prove the neutralization landed and each ARM red alone**; never read rc through a pipe. ⚠️ **Method records: `docs/knowledge/`, in the Knowledge Map — consult, do not re-derive.** ⚠️ **Suspect the FIXTURE before believing a number**: *if this were the gate, could the feature ever have worked?* RE-SEED one that depends on a defect, never delete it; a suite failing ENTIRELY, fast, is broken. ⚠️ ALWAYS `python3 -B scripts/project_env.py cargo …`; a bare `cargo` uses another CARGO_HOME.
- **Gates:** 18 checks; `DOCTRINE_ENFORCEMENT.md` mirrors them and `git log` names the leaf that added each. **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** the director's content-withheld report is prepared, NOT submitted (`target/extraction-input-controls/`, ignored, 0600).
- **Roadmap:** Phase 0–7 historical records exist; qualification is under corrective review, Phase 8 incomplete. Resume `PHASE-8.5.3` after its prerequisites, then `.5.4`, `.6`.
- **Defects/BLOCKERS:** findings owned in the tree; ⛔ no zero-defect claim. 🔴 **Blocker register `.13`**: the `Ack?` table heading `## Blockers` + `docs/book/src/blockers.md`. **RE-SURFACE every `Ack? = no` row EVERY stopping-point reply**; clear only on director engagement with THAT row.
- **Latest commit:** `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** **the book's CLI chapters carry the full recovery contract (RequestId/outcome after commit uncertainty, `--resume-bootstrap`) and its limits — read them rather than a copy here.** Semantic introspection/MCP remains the tracked `.6.4` proposal, no pivot.
- **Reading:** the full-source census is historical evidence at `9c2d2ba` (131 records), NOT a currency claim later. The Rust corpus exceeds one session; re-read the roadmap, the book and the leaf's sources in full, resuming the rest from the records.
- **Push cadence:** ~300-commit batches; the full rule and the ENDED red-remote exception are in `COMMIT.md`. ⛔ Do not push per commit.
- **PNT:** resumed by the director 2026-09-11 in full PNT mode. No background job in flight. Commit each bounded leaf; full CI before push.
- **Storage:** all project output/cache/temp stores are repository-derived, on its volume. Run `scripts/census_pg_test_clusters.py` (`--retire --confirm`) rather than rebuilding the judgement. ⛔ The build-cache disposition is `.11.4.3.1.8`'s — the cache Cargo does NOT collect is the larger one. Installed tools are read-only exceptions; use fresh ownership evidence, never a frozen manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 name clearance are open; none blocks local repairs.
