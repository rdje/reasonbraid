# MEMORY — resume pointer (layer A; overwrite-only, keep ≤ ~50 lines)

## How to resume

- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- Work is tracked in task-trees under `docs/tasks/`; follow `COMMIT.md`.
- Durable facts/decisions live in `docs/decisions/` (+ its `INDEX.md`).
- Transferable methods: `TOOLBOX.md` and `docs/knowledge/` — consult, do not re-derive.

## Current state (OVERWRITE this block each update — do not append)
- latest_commit: `REASONBRAID-REPAIR-0273` (leaf `SIGNOFF-REPAIR.7.1.2.1`): **the workflow takeover was OBSERVED before it was repaired** — Alice's thread ran Mallory's steps — and `workflow_register` is now a site capability (`migrations/0071`, following `0063`). Re-derive the ahead count with `git rev-list --count origin/main..HEAD`; push at ~300.
- active_work_unit: `SIGNOFF-REPAIR` → frontier leaf: `SIGNOFF-REPAIR.7.1.2.2` (`pending`). ✅ CLOSED this session: `.3.5.4`, `.3.5.5`, `.11.2.6`, `.4.2.3.1`, `.11.4.7.3`, `.11.4.7.4`, `.11.4.7`, `.7.3.6` + `.7.3.6.1`–`.7.3.6.5`, `.11.20.3`, `.7.1.1`, `.7.1.1.1`, `.7.1.2`, `.7.1.2.1` (+ DOC-0057–0066, REPAIR-0273). 🔎 OPENED: `.11.2.7`, `.11.22.1`, `.7.3.6.2`–`.7.3.6.5`, `.11.2.8`, `.7.1.1.1`, `.7.1.2`, `.7.1.2.1`, `.7.1.2.2`.
- next_action: `.7.1.2.2` — the routing journal. `routing_resolutions` and `routing_recommendations` each have exactly ONE product reader (`routing::list_resolutions` / `list_recommendations`) and neither carries a predicate of any kind, so every tenant reads every tenant's routing trail (`caller`, `surface`, `evidence_ref`). ⚠️ **A DISCLOSURE path, not a write path** — `routing::resolve` reads neither, so it binds nobody's outcome and DOC-0029's *tenant-bound read* remedy fits exactly. ⛔ Derive the tenant from the AUTHENTICATED CALLER, never from the wire (`docs/decisions/2026-09-18_node-presence-is-read-by-its-own-tenant.md`); `caller` is an actor handle, not a tenant. The `.3.5.5` POSITIVE arm is required: a tenant must still read its OWN journal in full, or a repair that refused everyone would pass.
- ⭐ **THE `.7.1.2` ADJUDICATION, because the next leaf must not re-derive it:** the 17 are **4 tenant-owned by derivation · 1 within DOC-0029 · 2 shared journals · 10 shared CONTROL surfaces**. ⚠️ **The residue is now SIXTEEN and the census reads 41/32 across 29 tables** — `.7.1.2.1` repaired `workflow_profiles` out of the `identity only` population, and `census_admission_paths.py` gained the `site authority` gate term that had made it call all ten site routes `identity only`. Record: `docs/decisions/2026-09-19_the-policy-registry-is-a-shared-control-surface.md`; instrument `scripts/census_registry_read_reach.py`, pinned by `.doctrine/registry_read_reach_baseline.tsv`. ⛔ **`site-global` means NO TENANT COLUMN, never NO OWNER** — 9 of 40 recover the tenant by a foreign key. ⛔ DOC-0029's remedy reaches no control surface: their problem is the WRITE. Still open: `.6.1.5` (re-scoped from 1 table to 10), `.8.2` (the evaluation family is SEVEN).
- ⭐ **`.7.1.2.1` settled the WORKFLOW registry and the same question is open for the POLICY registry.** `workflow_register` is a site capability now (`migrations/0071`, the `0063` template: widen both CHECK constraints, add the `Action`, wrap in `authorized(...)`). ⛔ The shape was chosen by the code's OWN words — `workflows::register`'s doc comment already read *"the operator's verb"* — not by preference; `.6.1.5` should decide the policy registry consistently or state the difference.
- in_flight_uncommitted: none — working tree clean, no background job owned by this repo (`bash scripts/check_no_background_jobs.sh`). ⛔ **BUILDS HERE ARE A HOST PROBLEM, not a tree-size one** (2026-09-19): a 68m 16s rebuild ran with `syspolicyd` at 44% CPU and swap at 6,151 of 7,168 MB. ✅ `target/debug` was RETIRED on the director's instruction (DOC-0060, `.11.4.3.1.8`) — **≈174 GiB by `df`** — so **the next build is cold by design**. ⛔ `cargo clean` must NOT be used: it would take `target/pg-tests`, `target/ci-browser` and `target/browser-lifetime-controls`, which are CITED evidence.
- ⛔ **THIS FILE IS A POINTER. A lesson lives in `docs/knowledge/`; this block names it and stops.** Restating one here is the `SIGNOFF-REPAIR.11.16` defect one layer up, and it is what took the file to 6,123 of 7,168 bytes (`.11.20`). Run `python3 -B scripts/census_memory_warnings.py` before evicting anything, and classify every `UNCITED` item by hand against the durable layer — that class is where a FACT, not a pointer, gets lost (`.11.20.1`, `.11.20.3`).
- ⭐ **THE LESSONS ARE A LAYER, NOT A LIST — enumerating them here is the defect bullet 1 names.** 41 notes in `docs/knowledge/`, indexed by the derived `KNOWLEDGE_MAP.md`; methods in `TOOLBOX.md`. The two most often needed: `a-control-that-passes-for-an-unrelated-reason` (in-situ falsification, and why a NEGATIVE arm needs a degenerate implementation) and `calibrate-over-the-history-that-contains-the-instance`.
- blockers: register `SIGNOFF-REPAIR.13` + `docs/book/src/blockers.md`. Column is `Owed here?`. ⛔ **B1, B2, B3 are `yes (deferred)` — director instruction 2026-09-18: Internet exposure is NOT high priority, the LAN must fully work first** (`docs/decisions/2026-09-18_lan-completeness-precedes-internet-exposure.md`). Work is still owed here and `.14` still owns it; it does NOT ride the frontier. ⭐ **The LAN bar is DEFINED: G0–G5 genuinely met + G7 earned on the LAN, G6 out of scope** — G6 is the only gate whose *unlocks* column names a transport. G7's numeric legs (load, SLO) are re-derived on the Internet; its structural legs are earned once. ✅ **C2 is CLOSED** (all five gate records re-derive; `.11.4.7`, REPAIR-0265) — so NO register row is work now; ✅ **G4's one un-discharged strand is DISCHARGED** (`.7.3.6.5`, REPAIR-0270 — the R3 `vm_container` claim corrected to `process`). ⛔ NOT a claim that G4 is Met: the record's *G4 must be re-earned* verdict is unchanged. **B4 `no`** (name clearance). **C1 `no`** — a limit on what may be CLAIMED. ⛔ Deferral changes priority, never claims: Phase 7 G6/G7 stays NOT MET and `.14`'s prohibition stands. A `yes` row rides the frontier; `yes (deferred)` and `no` are surfaced once per session with their trigger.
- ⭐ **DIRECTION, accepted and UNSCHEDULED (2026-09-18): small-swarm orchestration (2–5 agents) is the EXISTING objective, not a pivot; the binding constraint is the ACCEPTOR; the named first workload `rdje/bedrock` is a GitHub TEMPLATE and was PARKED the same day — DO NOT work it.** All of it, in more detail than a pointer can hold: `docs/decisions/2026-09-18_multi-agent-orchestration-is-the-objective-not-a-pivot.md`. It does NOT displace the LAN bar and no leaf is open.
- ⚠️ standing corpus rule from `.13.4`: **a correction is not complete until the LIVE-DOCUMENT corpus that restates it has been censused.** Live documents = `README.md`, `LIVE_STATUS.md`, `MEMORY.md`, `docs/book/src/*.md`; the dated ledgers are history and stay.
