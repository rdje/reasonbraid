# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public. Director correction 2026-09-09 supersedes the wrong README/ADR private instruction. Working-name clearance remains open and does not block public source visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Full local checkpoint PASSED at `7233122`; 339 commits pushed (`b932c05..a626768`). Today's R2 sequence: 0095 the acquisition success join + the `ApiState::with_acquisition` seam, 0096 the mismatch refusal's live absence (parent `.7.3.3.4` complete), 0097 the accept-set census + decision, 0098 the repair — the leg admits the RANKED pack's advertised media types (`.7.3.3.5` complete), 0099 the LOCKSTEP-CLAIM gate, DOC-0006 the second changelog rotation, 0100 the INDEX-FRONTIER gate (17 checks now). Earlier: 0069 checkpoint, 0070 book links, 0071 honest evidence storage faults, 0060/0063/0064 the production R2 input boundary, 0065 recreated-schema ACL, 0066 FILE-TERMINATION, 0067 clock-free fixture naming, 0068 distinct evidence identifiers, 0061/0062 visibility and book-frontier invariants. Per-leaf detail is in the tree and `LIVE_STATUS.md`; do not re-summarize it here.
- **Next action:** Frontier row 1 is `.3.3.4.3.3.3.3.2.3.2` — return to bounded CLI transport/reply recovery after the checkpoint: the HTTP client still has no connect or whole-request timeout, so a peer that never responds holds the live writer's local lock until the command is cancelled, and the request key must be preserved on timeout. Row 2 `.3.3.4.3.4` authority writer coverage. Then `.11.5`: failed-first control doctrine, adversarial concurrency lane, pipeline-stage registry, simulator.
- **Gates:** 17 registered checks. `.11.4.5.1` done (REPAIR-0094); `.11.4.5.2` done (REPAIR-0099: a ticked LOCKSTEP box may not name a live document the commit does not stage; `--against <sha>` re-runs it over any past commit); `.11.4.5.3` done (REPAIR-0100: `INDEX-FRONTIER` — the index may not name a frontier its own tree does not; the census rejected BOTH a blanket-equality rule and the generator); `.11.4.3.1.2.15` closed (REPAIR-0093); `.11.4.4` done (TASK-STATUS + HEADING-DEPTH). **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them. Service, CLI and HTTP enforcement are implemented and verified (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** director reports repeated content-withheld notices. Private prepared report: target/extraction-input-controls/openai-support-report.md (ignored, mode 0600); not submitted. Exact blocked-request IDs/trigger and account/access details remain unknown. Redacted durable incident record is in the current evidence artifact; no account or safety-setting changes.
- **Roadmap:** Phase 0–7 historical execution records exist; current qualification is under corrective review. Phase 8 remains incomplete. Resume `PHASE-8.5.3` after its corrective prerequisites, then `.5.4`, `.6`, and later executable work.
- **Defects:** open source-review findings owned by the repair tree; foreign-target revocation and repeated-revoke epoch defects reproduced; runner cleanup/spawn races reproduced and fixed. No zero-defect claim.
- **Latest commit:** derive with `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** server canonical RequestId/outcome recovery is qualified after actual commit uncertainty/disconnect. CLI now saves that key before HTTP, preserves matching requests through failure, strictly validates original replies, publishes principal/receipt and clears pending under one guard. Explicit --resume-bootstrap recovers pending/latest completion; completed receipts restore locally with labelled historical source, normal no-pending invocation stays fresh. Real unread-output interruption and server restoration/fresh intent pass. Prospective completion capacity is now checked before dispatch; later filesystem errors, live authority and endpoint/database continuity retain their limits. Semantic introspection/MCP remains tracked .6.4 proposal, no pivot.
- **Reading:** the original full-source census (roadmap, 284 tracked non-Markdown files, all mdBook sources) is historical evidence at review baseline `9c2d2ba`, preserved as the 131 records under `docs/tasks/artifacts/signoff_review/`; it is not a currency claim for later commits. The 2026-09-11 resumption re-read `ROADMAP.md` and all 17 mdBook sources in full, plus the sources its leaf touched. The tracked non-Markdown corpus is now 342 files / 3.53 MB of Rust; a fresh session cannot hold all of it and should re-read the roadmap, the book and its leaf's sources, resuming the rest from the census records.
- **Push cadence:** batches of ~300 commits after a passing local checkpoint (`COMMIT.md`). The red-remote exception ENDED on 2026-09-12 when run 34652116508 went green; do not push per commit.
- **PNT:** resumed by the director on 2026-09-11 in full PNT mode. No background job is in flight. Commit each bounded leaf and run full CI before push. Source f0265e2 was 310 commits ahead of confirmed remote main b932c054023ea127520e74cfaf95b9bdf1ea47fe.
- **Storage:** all project output/cache/temp stores must be repository-derived on its volume. use `python3 -B scripts/project_env.py <command>`; Make targets use it. Shared cache seed verified; installed tools are read-only exceptions. Artifact review is due on resumption; no deletion was attempted during handoff preparation. Use fresh identity/ownership evidence, never a frozen cleanup manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 public-name clearance remain open; they do not block local repairs.
