# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public. Director correction 2026-09-09 supersedes the wrong README/ADR private instruction. Working-name clearance remains open and does not block public source visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Full local checkpoint PASSED at `7233122`; 339 commits pushed (`b932c05..a626768`), so the local tree is ahead of the confirmed remote. The per-leaf sequence is NOT re-summarized here: `git log --oneline` names every `REASONBRAID-REPAIR-*` commit with its leaf id, the tree's Commit Log lists them against their leaves, and `LIVE_STATUS.md` carries what each one measured. Reconstruct it from those three, not from a copy that goes stale next commit.
- **Next action:** frontier row 1 is `.4.2.4` (`ack` writes on a pre-check), then `.4.2.7`, `.4.2.3.1`, `.11.9`, `.11.7`. ⚠️ **The frontier is a curated shortlist, not the remaining work: 37 leaves are `pending`** — it fell to one row on 2026-09-13 and was refilled, because a short frontier reads as an exhausted tree. 🔴 **`.4.2` split into SEVEN; `.4.2.1`/`.4.2.6`/`.4.2.2`/`.4.2.3` closed.** ⛔ **ALL THREE renewal conditions ride the WRITE** (epoch `.2.2`, credential `.4.1.3`, expiry `.4.2.3`); the lease reads the **DB clock** like `node_presence.online` but is WRITTEN process-side, so 60 s TTL is nominal (`.4.2.3.1`). ⚠️ **A refusal's WORDING is a repair part** — an answer that changes with the scheduler is not a contract. ⛔ **Proof nonces consumed once, AFTER the signature**; `nonce` REQUIRED on handshake/rotate (`422` if absent). ⚠️ **An FK on a bookkeeping table DEADLOCKED the auth path**; a HANGING suite is a third outcome beside red and green — read the process table. 🔴 **Revocation is bounded by the LEASE** (`.4.1.3`, `.4.1.3.1`): only `handshake`/`rotate` re-present a certificate, so the RENEWAL check ends a revoked session and DELIVERY (`replay` — `poll` AND the handshake) asks the same question, so the tail gets no new work: withheld, NOT dropped, so a replacement still receives it. ⛔ `node_presence.suspended` is NOT that question — **0017 supersedes 0012** and never asks whether the survivor is IN DATE. ⛔ **A partial index predicate must be IMMUTABLE** — `now()` is not, so liveness an index must key on has to be WRITTEN DOWN (`.4.1.1`: `superseded_at`; an EXPIRED token still satisfies `used_at IS NULL`). ⛔ **`nodes.node_id` is a GLOBAL primary key — do NOT make the token index per-tenant** (`.3.5.1`): it moves a clean `409` into a redemption-time key violation. ⛔ **`.3.5.2.1` is HELD for a director decision** (auditing the metrics read). ⛔ **Do not reintroduce `.3.4.3`'s `min(decided_at, received_at)` clamp.** 🔴 **DELEGATION IS ONE HOP DEEP EVERYWHERE**: ⛔ do not "fix" `delegable`/`max_delegation_depth`, nor read a chain into the envelope. ⚠️ **Widening a guard changes which BRANCH runs next** — check what you widen INTO (`.3.5.1`). ⚠️ **A census record routed to a leaf may not have survived that leaf's SPLIT** (`.11.9`: 1 of 3 clauses did). ⚠️ **Scope a census to `crates/` WHOLE** — a fixture list lives in `reasonbraid-mcp/src/lib.rs`. ⛔ **A token dies with its issuer's grant/boundary** (`.4.1.2`: voided at REVOCATION — `enroll` holds no tenant guard); TTL capped at 86 400 s (`.4.1.2.1`). ⚠️ **Rank a census by REACHABILITY, not resemblance**; "not reachable" usually means *in production*. ⛔ **46 mutating routes are NOT repaired, all owned.** ⚠️ **Falsify each part SEPARATELY, and PROVE the neutralization landed**; never read an exit code through a pipe. ⚠️ **Method records: `docs/knowledge/`, indexed in the Knowledge Map — consult, do not re-derive.** ⚠️ **Suspect the FIXTURE before believing a number**: *if this were the gate, could the shipped feature ever have worked?* Never delete one that depends on a defect; a suite failing ENTIRELY, fast, is broken. ⚠️ ALWAYS `python3 -B scripts/project_env.py cargo …`; a bare `cargo` uses another CARGO_HOME.
- **Gates:** 18 registered checks; `DOCTRINE_ENFORCEMENT.md` is the readable mirror and `git log` names the leaf that added each. **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them. Service, CLI and HTTP enforcement are verified (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** the director's content-withheld report is prepared and NOT submitted (`target/extraction-input-controls/openai-support-report.md`, ignored, 0600); the redacted record and its detail are in the current evidence artifact. Blocked-request IDs unknown; no account change.
- **Roadmap:** Phase 0–7 historical records exist; qualification is under corrective review, Phase 8 incomplete. Resume `PHASE-8.5.3` after its prerequisites, then `.5.4`, `.6`.
- **Defects:** open findings are owned by the repair tree and enumerated there. ⛔ No zero-defect claim.
- **Latest commit:** derive with `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** canonical RequestId/outcome recovery is qualified after commit uncertainty/disconnect; `--resume-bootstrap` recovers pending or the latest completion. **The book's CLI chapters carry the full contract and its limits — read them rather than a copy here.** Semantic introspection/MCP remains the tracked `.6.4` proposal, no pivot.
- **Reading:** the full-source census is historical evidence at baseline `9c2d2ba` — 131 records under `docs/tasks/artifacts/signoff_review/`, NOT a currency claim for later commits. The Rust corpus exceeds one session; re-read the roadmap, the book and the leaf's own sources in full, resuming the rest from the records.
- **Push cadence:** batches of ~300 commits after a passing local checkpoint (`COMMIT.md`). The red-remote exception ENDED on 2026-09-12 when run 34652116508 went green; do not push per commit.
- **PNT:** resumed by the director 2026-09-11 in full PNT mode. No background job in flight. Commit each bounded leaf; full CI before push.
- **Storage:** all project output/cache/temp stores must be repository-derived on its volume. Run `scripts/census_pg_test_clusters.py` (`--retire --confirm`) rather than rebuilding the judgement; last run 2026-09-13 reclaimed 2.18 GB, 5 clusters kept. ⛔ The build-cache disposition is `.11.4.3.1.8`'s — the cache Cargo does NOT collect is the larger one. Installed tools are read-only exceptions; use fresh ownership evidence, never a frozen manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 public-name clearance remain open; they do not block local repairs.
