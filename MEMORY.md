# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public. Director correction 2026-09-09 supersedes the wrong README/ADR private instruction. Working-name clearance remains open and does not block public source visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Full local checkpoint PASSED at `7233122`; 339 commits pushed (`b932c05..a626768`), so the local tree is ahead of the confirmed remote. The per-leaf sequence is NOT re-summarized here: `git log --oneline` names every `REASONBRAID-REPAIR-*` commit with its leaf id, the tree's Commit Log lists them against their leaves, and `LIVE_STATUS.md` carries what each one measured. Reconstruct the recent sequence from those three rather than from a copy that goes stale the next commit.
- **Next action:** frontier row 1 is `.3.4.3.1.2` — let the node evaluate server instants in the server's terms; it CLOSES the dispatch outage. `.3.4.3.1.1` narrowed it: of the three instants sent, **only `decided_at` is read**; the node's other comparison uses the certificate's embedded `not_after`, not a wire field — two values, two clocks. The server's two clocks are DECLARED coherent within 1 s, measured by a control (⚠️ CI, one host). Then `.3.4.3.1.3`, `.3.5`, `.11.8`, `.11.6`. **Read the owning leaf first** — it holds the census and what it may not claim. ⛔ **Dispatch outage OPEN: a node clock AHEAD refuses EVERY dispatch past 60 s of skew**; a node BEHIND >300 s rotates its cert only after expiry (`.3.4.3.1.3`, NOT reproduced). ⭐ The answer's shape ships one file away: `worker.rs` anchors a server DURATION to its own clock. ✅ **`.3.4` is CLOSED (0131–0136).** 🔴 **DELEGATION IS ONE HOP DEEP EVERYWHERE** (measured three ways in `.3.4.1`/`.1.1`/`.5`); its vocabulary is written for something larger. ⛔ Do not "fix" `delegable`/`max_delegation_depth`; do not read a chain into the envelope. ⚠️ **A stored value describing an artifact must be READ from it, not computed alongside** — `.3.4.3.1.1`'s expiry disagreed on every issuance. ⚠️ **Rank a census by REACHABILITY, not resemblance**; "not reachable" usually means *in production*. ⚠️ **A correction you are confident about often rests on an unmeasured assumption one layer DOWN** — `.3.4.3`'s clamp broke replay; `.3.4.3.1`'s offset assumed two clocks agreed. ⛔ **46 mutating routes are NOT repaired, each with a named owner.** ⚠️ **Falsify each part of a multi-part fix SEPARATELY**; **never read an exit code through a pipe**. ⚠️ **DERIVE the guard mode and fixture each time**; the fixture follows the lock-mode TRANSITION — if none discriminates, say so. ⚠️ **The method records are in `docs/knowledge/`, indexed in the Knowledge Map — consult them rather than re-deriving**; run the enumerate-the-constraints check before making a route atomic. ⚠️ **Suspect the FIXTURE before believing a number**: ask *if this were the gate, could the shipped feature ever have worked?*, and check it before explaining an irregular measurement. Never delete a fixture that depends on a defect — correct it; a suite failing ENTIRELY, fast, is broken. ⚠️ ALWAYS `python3 -B scripts/project_env.py cargo …`; a bare `cargo` uses another CARGO_HOME.
- **Gates:** 17 registered checks; `DOCTRINE_ENFORCEMENT.md` is the readable mirror and `git log` names the leaf that added each. **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them. Service, CLI and HTTP enforcement are implemented and verified (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** director reports repeated content-withheld notices; the prepared private report (target/extraction-input-controls/openai-support-report.md, ignored, 0600) is NOT submitted, and the redacted durable record is in the current evidence artifact. Blocked-request IDs and account details remain unknown; no account or safety-setting change was made.
- **Roadmap:** Phase 0–7 historical execution records exist; current qualification is under corrective review. Phase 8 remains incomplete. Resume `PHASE-8.5.3` after its corrective prerequisites, then `.5.4`, `.6`, and later executable work.
- **Defects:** open source-review findings owned by the repair tree; foreign-target revocation and repeated-revoke epoch defects reproduced; runner cleanup/spawn races reproduced and fixed. No zero-defect claim.
- **Latest commit:** derive with `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** server canonical RequestId/outcome recovery is qualified after actual commit uncertainty/disconnect; the CLI saves that key before HTTP, validates original replies strictly, publishes principal/receipt and clears pending under one guard, and `--resume-bootstrap` recovers pending or the latest completion. Completion capacity is checked before dispatch. The book's CLI chapters carry the full contract and its limits. Semantic introspection/MCP remains the tracked `.6.4` proposal, no pivot.
- **Reading:** the original full-source census is historical evidence at review baseline `9c2d2ba`, preserved as the 131 records under `docs/tasks/artifacts/signoff_review/`; it is not a currency claim for later commits. The tracked Rust corpus exceeds what one session can hold; re-read the roadmap, the book and the leaf's own sources in full, resuming the rest from the census records.
- **Push cadence:** batches of ~300 commits after a passing local checkpoint (`COMMIT.md`). The red-remote exception ENDED on 2026-09-12 when run 34652116508 went green; do not push per commit.
- **PNT:** resumed by the director on 2026-09-11 in full PNT mode. No background job is in flight. Commit each bounded leaf and run full CI before push.
- **Storage:** all project output/cache/temp stores must be repository-derived on its volume. The §8 review now has a tracked instrument, `scripts/census_pg_test_clusters.py` (`--self-test`, `--retire --confirm`); run it rather than rebuilding the judgement. Measured 2026-09-12: `target/debug/deps` 134,478,136 KiB vs `incremental` 58,989,732 KiB — the cache Cargo does NOT collect is the larger one, and its disposition is `.11.4.3.1.8`, untouched until then. use `python3 -B scripts/project_env.py <command>`; Make targets use it. Shared cache seed verified; installed tools are read-only exceptions. Artifact review is due on resumption; no deletion was attempted during handoff preparation. Use fresh identity/ownership evidence, never a frozen cleanup manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 public-name clearance remain open; they do not block local repairs.
