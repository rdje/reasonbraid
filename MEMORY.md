# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public (director correction 2026-09-09 supersedes the wrong README/ADR private instruction). Working-name clearance is open and does not block public visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Full local checkpoint PASSED at `7233122`; 339 commits pushed (`b932c05..a626768`), so local is ahead of the confirmed remote. ⚠️ **46 leaves are pending** — re-derive with the frontier caption's awk command, which reads BOTH `- Status:` and the `- Opened:`-only spelling (11 carry only the latter). The per-leaf sequence is NOT re-summarized here: reconstruct it from `git log --oneline`, the tree's Commit Log and `LIVE_STATUS.md`.
- **Next action:** frontier row 1 is `.11.9.1.1.3` (tranche 2c), row 1b `.11.11`, then `.3.5.4`. 🔴 **A LINE NUMBER into a growing file is `LIVE-DOC-CURRENCY` in another costume — locate by HEADING** (graded 2026-09-14: 2 of 8 stale within the hour; every number from a command held). ⚠️ **Say when a measurement is UNSTABLE**, not only what it was. ⛔ **ASK THE LIBRARY, never restate it** (`.11.9.1.1.2`, `.4.1.6`): measure it when the SOURCE cannot settle a clause, and validate a caller string where a HUMAN typed it against the library's OWN verdict — narrows nothing, cannot drift. ⚠️ N probed inputs are a SAMPLE, never the boundary. ⚠️ A type-changing repair cannot be falsified by reverting the file — the control will not compile; restore only the superseded CLASSIFICATION under the new signature. ⚠️ **SCOPE A CENSUS WHOLE**: a `grep -v '^docs/'` filter hid 2 of `.11.2.2`'s 7 sites (published 5), and a fixture list once hid in `reasonbraid-mcp/src/lib.rs`; ⛔ `git grep -- 'crates/*/src'` silently matches NOTHING. ⛔ **Two caller-supplied identifiers must be BOUND, not one checked** (`.3.5.3`: admitted on the caller's tenant, selected on `node_id`); `inspect_call` is the right shape (tenant DERIVED from the target). `.3.5.4` owns the 10 delegating GET handlers. ⭐ **Before evicting a warning here, run `scripts/census_memory_warnings.py`** (`.11.4.2.1`: 26 warnings, 0 exist only here — shed one whose line NAMES its leaf). ⛔ **A record's candidate list is a SUGGESTION** (`.11.9`); ⭐ **rank that backlog by its NARROWEST candidate, NEVER by fan-out** — `.11.9.1` measured the two ANTI-correlated (`.11.4` is named by 99 of 131; `R-6-27-1` has NO finding and still got a leaf). Ledger, vocabulary and the ⭐ **`attach`** state — a clause the owner's OWN TEXT does not make visible, which its split will drop — are in `signoff_review/RECONCILIATION.md`; `--rank`/`--classified` re-derive them. 🔴 **`attach`: write the sentence into the owning leaf IN THE SAME COMMIT** — the only state whose next action is not "none", `--classified` cannot see it, and tranche 1 recorded three and wrote zero (`.11.9.1.1.1`; the gate is `.11.11`). 🔴 **A guard asking "does a usable credential exist for this ID?" is satisfied by ANY — including one issued AFTER the event it defends against**: a replacement re-satisfied `.4.1.3` (`.4.1.5`). A replacement now ENDS the lease (epoch BUMPED, never DELETED — that resets it to 1), the host and the incarnation. ⛔ **EVERY channel WRITE re-checks the lease IN-TX, row locked** (`.2.2`/`.4.2.3`/`.4.2.4`); ⛔ **`poll` NOT**; ⛔ **no tenant guard on `ack`**. The lease reads the **DB clock** like `node_presence.online` but is WRITTEN process-side, so 60 s TTL is nominal (`.4.2.3.1`). ⛔ **Proof nonces consumed once, AFTER the signature**; `nonce` REQUIRED on handshake/rotate. 🔴 **DELEGATION IS ONE HOP DEEP**: ⛔ do not "fix" `delegable`/`max_delegation_depth` nor read a chain into the envelope. ⚠️ **Rank a census by REACHABILITY, not resemblance**; "not reachable" usually means *in production*. ⚠️ **Falsify each part SEPARATELY, prove the neutralization landed, and prove a later ARM goes red on its own**; never read rc through a pipe. ⚠️ **Method records: `docs/knowledge/`, in the Knowledge Map — consult, do not re-derive.** ⚠️ **Suspect the FIXTURE before believing a number**: *if this were the gate, could the feature ever have worked?* Never delete one that depends on a defect; a suite failing ENTIRELY, fast, is broken. ⚠️ ALWAYS `python3 -B scripts/project_env.py cargo …`; a bare `cargo` uses another CARGO_HOME.
- **Gates:** 18 checks; `DOCTRINE_ENFORCEMENT.md` mirrors them and `git log` names the leaf that added each. **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** the director's content-withheld report is prepared, NOT submitted (`target/extraction-input-controls/`, ignored, 0600).
- **Roadmap:** Phase 0–7 historical records exist; qualification is under corrective review, Phase 8 incomplete. Resume `PHASE-8.5.3` after its prerequisites, then `.5.4`, `.6`.
- **Defects:** open findings are owned and enumerated in the repair tree. ⛔ No zero-defect claim.
- **Latest commit:** derive with `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** **the book's CLI chapters carry the full recovery contract (RequestId/outcome after commit uncertainty, `--resume-bootstrap`) and its limits — read them rather than a copy here.** Semantic introspection/MCP remains the tracked `.6.4` proposal, no pivot.
- **Reading:** the full-source census is historical evidence at baseline `9c2d2ba` — 131 records, NOT a currency claim for later commits. The Rust corpus exceeds one session; re-read the roadmap, the book and the leaf's own sources in full, resuming the rest from the records.
- **Push cadence:** batches of ~300 commits after a passing local checkpoint (`COMMIT.md`). The red-remote exception ENDED on 2026-09-12 when run 34652116508 went green; do not push per commit.
- **PNT:** resumed by the director 2026-09-11 in full PNT mode. No background job in flight. Commit each bounded leaf; full CI before push.
- **Storage:** all project output/cache/temp stores must be repository-derived on its volume. Run `scripts/census_pg_test_clusters.py` (`--retire --confirm`) rather than rebuilding the judgement. ⛔ The build-cache disposition is `.11.4.3.1.8`'s — the cache Cargo does NOT collect is the larger one. Installed tools are read-only exceptions; use fresh ownership evidence, never a frozen manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 name clearance are open; none blocks local repairs.
