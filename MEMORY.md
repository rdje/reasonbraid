# MEMORY — resume pointer (layer A; overwrite-only, keep ≤ ~50 lines)

## How to resume

- Read `MEMORY_ARCHITECTURE.md` (the memory system) and `README.md` (the project).
- Work is tracked in task-trees under `docs/tasks/`; follow `COMMIT.md`.
- Durable facts/decisions live in `docs/decisions/` (+ its `INDEX.md`).
- Transferable methods: `TOOLBOX.md` and `docs/knowledge/` — consult, do not re-derive.

## Current state (OVERWRITE this block each update — do not append)

- latest_commit: `REASONBRAID-REPAIR-0235` (leaf `SIGNOFF-REPAIR.11.18`): the section scanner did not know what Markdown is. Re-derive the ahead-of-origin count with `git rev-list --count origin/main..HEAD`; push at ~300.
- active_work_unit: `SIGNOFF-REPAIR` → frontier leaf: `SIGNOFF-REPAIR.11.17` (`pending`). ✅ The `.11.14.3` evidence-chain family is CLOSED; ✅ `.11.18` is CLOSED.
- next_action: `.11.17` — a published `file:line` is a bare basename and **29 of them name two files**. ⭐ The urgency was re-demonstrated by `.11.18`, which found two of its OWN pinned lines had moved `:4510`/`:4514` → `:4573`/`:4577` inside a single session. `CLAIM_VERIFICATION.md` §4.1 grades a NAMED INSTANCE as exact with no tolerance band, so a citation pinned to one commit and measured at another is a leg-1 breach. ⛔ Read the leaf's existing classified census first — it is measured as of REPAIR-0218's tree and will have moved.
- in_flight_uncommitted: none — working tree clean, no background job.
- standing lesson from 2026-09-17: ⭐ **A CONTROL THAT PASSES FOR AN UNRELATED REASON — three times today, and all three caught by one question:** *what would this have done if the defect were present?* A **scope** that silently skips what it cannot read (`FIXTURE-PLAN-CHILDREN`); a **stimulus** that never landed (`an-injection-must-be-shown-to-land`); a **discriminator** keyed on the wrong feature (arm 12 turned on indentation, not the fence). ⭐ The mechanical form: **run the NEW control against the OLD code, in situ** — every arm claiming to cover the defect must FAIL by name, and any arm that passes both ways must be LABELLED. ⭐ Also earned: **a bug's DIRECTION is a claim, not a symmetry** (`a-scoping-defect-errs-in-one-direction`), and **a row keyed on CONTENT may not hold a tenant's DECISION** (`a-shared-row-may-not-hold-a-tenant-decision`). ⛔ And the older one still binds: knowing a rule is not applying it — run a new rule over the instances that came BEFORE it.
- blockers: the register's column is now **`Owed here?`**, set by ME from the owning leaf — `.13.5` (DOC-0043) replaced `Ack?`, which asked whether the DIRECTOR had engaged and was `no` on every open row for its whole life. ⭐ **B1, B2, B3 are `yes` (work owed HERE)** — all three share `SIGNOFF-REPAIR.14`'s frozen exposure candidate, which the blockers page already said; reporting them as "OUTSIDE" hid my own work from me. **B4 `no`** (professional name clearance — the only row with nothing owed here). **C2 `yes`** (four gate records remain). **C1 `no`** and relabelled: not a blocker, a limit on what may be CLAIMED. Rule: a `yes` row is WORK and rides the frontier; a `no` row is surfaced once per session with its trigger, not in every reply. Register: `SIGNOFF-REPAIR.13` + `docs/book/src/blockers.md`.
- ⚠️ standing corpus rule from `.13.4` (2026-09-17): **a correction is not complete until the LIVE-DOCUMENT corpus that restates it has been censused** — `.13.3` corrected C1 in the register, the tree and the blockers chapter and left it asserted in `LIVE_STATUS.md` (×3) and the book's qualification chapter. Live documents = `README.md`, `LIVE_STATUS.md`, `MEMORY.md`, `docs/book/src/*.md`; the dated ledgers are history and stay. Routed to `.11.16`.
