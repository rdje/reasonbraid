# MEMORY — resume pointer

## How to resume

1. Read `CLAUDE.md`, `README.md`, `MEMORY_ARCHITECTURE.md`, `TOOLBOX.md`, `DOCTRINE_ENFORCEMENT.md`, `COMMIT.md`.
2. Open `docs/tasks/SIGNOFF-REPAIR.md` and its Current Frontier.
3. Source-review records: `docs/tasks/artifacts/signoff_review/INDEX.md`.

## Current state

- **Project:** ReasonBraid; public repository rdje/reasonbraid must remain public (director correction 2026-09-09 supersedes the wrong README/ADR private instruction). Working-name clearance is open and does not block public visibility.
- **Active tree:** `SIGNOFF-REPAIR`. Full local checkpoint PASSED at `7233122`; 339 commits pushed (`b932c05..a626768`), so local is ahead of the confirmed remote. The per-leaf sequence is NOT re-summarized here: `git log --oneline` names every `REASONBRAID-REPAIR-*` commit with its leaf, the tree's Commit Log lists them, and `LIVE_STATUS.md` carries what each measured. Reconstruct it from those three, not from a copy that goes stale next commit.
- **Next action:** frontier row 1 is `.11.9.1` (classify 114 uncited records), then `.11.7.1`, `.4.2.9`, `.4.2.3.1`. ⭐ **Before evicting a warning here, run `scripts/census_memory_warnings.py`** (`.11.4.2.1`: 26 warnings, 0 exist only here — shed one whose line NAMES its leaf). ⛔ **AUTHORIZATION NEVER DEPENDS ON PRINCIPAL KIND** — `tenant_admin` lets an AGENT issue enrollment tokens and revoke nodes (`.4.1.4`, decision record); the lever is the GRANT, never an endpoint check. ⛔ **A record's candidate list is a SUGGESTION, not an assignment** (`.11.9`; backlog `.11.9.1`). 🔴 **CITATION IS NOT ACCOUNTING**: `.4.2` cited all 15 of its records and still dropped SIX CLAUSES — reconcile record BODIES clause by clause at every split AND closure. ⚠️ **FALSIFY THE ACTUAL SUPERSEDED MECHANISM** — breaking a DIFFERENT thing fails just as loudly; a magnitude far larger than the real defect is the tell (`.4.2.8`: 4193 vs 242). ⭐ **Measure what a proposed gate would FIRE ON**: `.11.9`'s 114/131 → REJECTED; `.11.7`'s 0 today, 9 historically → REGISTERED. ⛔ **`ROADMAP.md` is FROZEN at v0.4.1** — measure and ROUTE (`.11.7.1`), never edit §9.8 from a repair leaf. ⚠️ **The frontier is a curated shortlist, not the remaining work: 39 leaves are `pending`** — it fell to one row on 2026-09-13 and was refilled, because a short frontier reads as an exhausted tree. ⭐ **Narrow a claim rather than widen code nothing can falsify yet; make a warning a TRIPWIRE, not a comment.** ⛔ **A `Result` the body can PANIC past is the defect** (`.4.1.2.1`, `.4.2.7`; promoted); ⛔ **EVERY channel WRITE re-checks the lease IN-TX, row locked** (`.2.2`/`.4.2.3`/`.4.2.4`); ⛔ **`poll` NOT** (all reads); ⛔ **no tenant guard on `ack`** (it cuts `.4.1.3.1`'s tail). ⚠️ `acknowledged_at` is the PRUNE's delete predicate. The lease reads the **DB clock** like `node_presence.online` but is WRITTEN process-side, so 60 s TTL is nominal (`.4.2.3.1`). ⚠️ **A refusal's WORDING is a repair part** — an answer that changes with the scheduler is not a contract. ⛔ **Proof nonces consumed once, AFTER the signature**; `nonce` REQUIRED on handshake/rotate. 🔴 **Revocation is bounded by the LEASE** (`.4.1.3`/`.4.1.3.1`): only `handshake`/`rotate` re-present a certificate, so RENEWAL ends a revoked session and DELIVERY (`replay`, from BOTH `poll` and the handshake) asks the same question; the tail is WITHHELD, not dropped. ⛔ `node_presence.suspended` is NOT that question — **0017 supersedes 0012**. ⛔ **Do not reintroduce `.3.4.3`'s `min(decided_at, received_at)` clamp.** 🔴 **DELEGATION IS ONE HOP DEEP**: ⛔ do not "fix" `delegable`/`max_delegation_depth` nor read a chain into the envelope. ⚠️ Scope a census to `crates/` WHOLE — a fixture list lives in `reasonbraid-mcp/src/lib.rs`; ⛔ `git grep -- 'crates/*/src'` silently matches NOTHING. ⚠️ **Rank a census by REACHABILITY, not resemblance**; "not reachable" usually means *in production*. ⚠️ **Falsify each part SEPARATELY, prove the neutralization landed, and prove a later ARM goes red on its own**; never read rc through a pipe. ⚠️ **Method records: `docs/knowledge/`, in the Knowledge Map — consult, do not re-derive.** ⚠️ **Suspect the FIXTURE before believing a number**: *if this were the gate, could the feature ever have worked?* Never delete one that depends on a defect; a suite failing ENTIRELY, fast, is broken. ⚠️ ALWAYS `python3 -B scripts/project_env.py cargo …`; a bare `cargo` uses another CARGO_HOME.
- **Gates:** 18 checks; `DOCTRINE_ENFORCEMENT.md` mirrors them and `git log` names the leaf that added each. **The REMOTE run is the authoritative pre-push gate** — before a push run only `make gate`, `make book`, `cargo fmt --all -- --check` and the Python controls.
- **Architecture decision:** shared registries require explicit site-operator grants; tenant enrollment cannot mint them; service/CLI/HTTP enforcement verified (`docs/decisions/2026-09-09_site-operator-authority.md`).
- **Support request:** the director's content-withheld report is prepared and NOT submitted (`target/extraction-input-controls/openai-support-report.md`, ignored, 0600); the redacted record and its detail are in the current evidence artifact.
- **Roadmap:** Phase 0–7 historical records exist; qualification is under corrective review, Phase 8 incomplete. Resume `PHASE-8.5.3` after its prerequisites, then `.5.4`, `.6`.
- **Defects:** open findings are owned and enumerated in the repair tree. ⛔ No zero-defect claim.
- **Latest commit:** derive with `git log -1 --oneline`; review baseline `9c2d2ba`.
- **Continuity:** **the book's CLI chapters carry the full recovery contract (RequestId/outcome after commit uncertainty, `--resume-bootstrap`) and its limits — read them rather than a copy here.** Semantic introspection/MCP remains the tracked `.6.4` proposal, no pivot.
- **Reading:** the full-source census is historical evidence at baseline `9c2d2ba` — 131 records under `docs/tasks/artifacts/signoff_review/`, NOT a currency claim for later commits. The Rust corpus exceeds one session; re-read the roadmap, the book and the leaf's own sources in full, resuming the rest from the records.
- **Push cadence:** batches of ~300 commits after a passing local checkpoint (`COMMIT.md`). The red-remote exception ENDED on 2026-09-12 when run 34652116508 went green; do not push per commit.
- **PNT:** resumed by the director 2026-09-11 in full PNT mode. No background job in flight. Commit each bounded leaf; full CI before push.
- **Storage:** all project output/cache/temp stores must be repository-derived on its volume. Run `scripts/census_pg_test_clusters.py` (`--retire --confirm`) rather than rebuilding the judgement. ⛔ The build-cache disposition is `.11.4.3.1.8`'s — the cache Cargo does NOT collect is the larger one. Installed tools are read-only exceptions; use fresh ownership evidence, never a frozen manifest.
- **External gates:** G6/G7 Internet qualification, license choice and ADR-001 name clearance are open; none blocks local repairs.
