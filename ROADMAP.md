# ROADMAP — reasonbraid

> ⛒ **DROP-IN FILE.** This is the one file that makes the project *yours*. Replace
> everything below with your project's real roadmap, then run `scripts/bootstrap.sh` to seed
> the first task-tree(s) from it. Everything else in this repo is the reusable discipline
> spine — the project grows from here with that spine as its backbone.


## Objective

_One paragraph: what this project delivers, for whom, and the single measure of "done"._

## Milestones / lanes

1. _(lane 1 — the first shippable slice)_
2. _(lane 2)_
3. _(lane 3)_

Each lane becomes one or more task-trees under `docs/tasks/`. Break a lane into leaves small
enough to finish, verify, and commit as one signoff-quality slice.

## Non-goals / out of scope (for now)

- _(what this project deliberately does not solve yet)_

## Constraints & principles

- _(hard constraints — performance budgets, correctness bars, environment limits — that
  every slice must respect. These often become `docs/decisions/` records and project-specific
  doctrine checks in `scripts/check_doctrines.project.sh`.)_
