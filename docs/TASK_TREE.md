# Task-Tree Workflow

This document defines the repo-local task-tree workflow. A step-by-step setup guide is in
[TASK_TREE_README.md](TASK_TREE_README.md). Individual trees live under
[`tasks/`](tasks/); the leaf template is [`tasks/TEMPLATE.md`](tasks/TEMPLATE.md).

## Purpose

Use a task tree when a top-level task is too broad to finish safely as one signoff-quality
slice, or when it is expected to discover subtasks over time. The tree owns the recursive
breakdown, current frontier, acceptance criteria, blockers, decisions, validation, and
completion evidence for one top-level task — so the project survives a lost session and
continuity holds across sessions, machines, and harness switches.

The tree is not a second roadmap. `ROADMAP.md` states the high-level direction; a task tree
owns the disciplined execution of one lane of it.

## Code-change doctrine (binding, non-negotiable)

**It is strictly forbidden to make any code change unless it is first tracked/owned by a
task-tree leaf.** "Code change" = any edit to Rust sources, `Cargo.toml`/build scripts,
generated artifacts, or anything altering behavior. Before touching code, a leaf must exist
that owns the change (create/extend a tree, or add a leaf). The leaf — its goal, acceptance,
verification, and commit — is the unit of traceability. Enforced by
`scripts/check_task_tree_ownership.sh`.

## Leaf lifecycle (statuses)

`proposed` → `pending` → `active`/`in_progress` → `done`. A leaf is `done` only when its
acceptance criteria are met, verification is recorded, and it is committed via `COMMIT.md`.
Mark `blocked` (with the blocker named) rather than leaving a stalled leaf `active`.

## The pivot rule

**Do not pivot to a different task-tree while the repo is dirty.** The repo is
handoff-ready only when the tree is clean (no modified/untracked work except the task-tree
file itself). Finish the current leaf and get the repo clean before switching — even if
asked to pivot immediately. The guarantor of repo integrity holds this line.

## Commit traceability

Each slice uses a work-unit id in the commit subject (e.g. `MYPROJ-AREA-0007`). When the
slice belongs to a leaf, the subject or first body line also names the leaf ID
(e.g. `MYPROJ-AREA-0007 (leaf FEATURE-X.2): …`), so the slice id and the tree node coexist
on the same commit. One commit per completed leaf.

## Active Task Trees

| Tree | Status | Frontier (next leaf) | Owner |
| --- | --- | --- | --- |
| [`BOOTSTRAP`](tasks/BOOTSTRAP.md) | `done` | `.1` — bootstrapped from the ReasonBraid spine | repo-local |
| [`RB-SEED`](tasks/RB-SEED.md) | `done` | `.3` — CLAIM_VERIFICATION adopted | repo-local |
| [`PROGRAM`](tasks/PROGRAM.md) | `active` | index only — current executable tree is `SIGNOFF-REPAIR` | repo-local |
| [`PHASE-0`](tasks/PHASE-0.md) | `done` | tree complete — WP1–WP8 + `MAINT-1`/`MAINT-2`; next executable work is `PHASE-1.1` | repo-local |
| [`PHASE-1`](tasks/PHASE-1.md) | `done` | tree complete — G1–G2 **Met** + Demonstration A passed 30/30 (debug + release-built); next executable work is `PHASE-2.1` | repo-local |
| [`PHASE-2`](tasks/PHASE-2.md) | `done` | tree complete — the exit line's properties measured (non-escalation, restore + replacement, no false safe-retry) + the subtraction record; next executable work is `PHASE-3.1` | repo-local |
| [`PHASE-3`](tasks/PHASE-3.md) | `done` | tree complete — the directory, the presence, the matching, the recruitment, the subscriptions, the dependence indicators; next executable work is `PHASE-4.1` | repo-local |
| [`PHASE-4`](tasks/PHASE-4.md) | `done` | tree complete — the five pack lanes + the evidence pipeline; the G4 gate Met (the record + the subtraction + the manifest) | repo-local |
| [`PHASE-5`](tasks/PHASE-5.md) | `done` | tree complete — the five lanes + the G5 gate **Met as a subtraction gate** (the lift claim withdrawn per §25.1; the record + the subtraction + the manifest); next executable work is `PHASE-6.1` | repo-local |
| [`PHASE-6`](tasks/PHASE-6.md) | `done` | tree complete — the seven lanes + the G3 gate **Met as machinery, blocked as binding use** (the record + the subtraction + the Demo-B walk + the manifest); next executable work is `PHASE-7.1` | repo-local |
| [`PHASE-7`](tasks/PHASE-7.md) | `done` | tree complete — the five lanes + the G6–G7 gate package (**NOT MET for the Internet exposure, Met as the hardening-machinery exit** — the gate record + the subtraction + the unsupported matrix + the manifest); next executable work is `PHASE-8.1` | repo-local |
| [`SIGNOFF-REPAIR`](tasks/SIGNOFF-REPAIR.md) | `active` | `.11.14.1` — 🔴 **any enrolled principal can enumerate every tenant's evidence trail.** ⭐ **`SIGNOFF-REPAIR.14` IS OPEN (DOC-0030): the exposure-profile candidate, built and frozen, NEVER deployed** — the single prerequisite B1, B2 and B3 share, and it is in-repo work. The subtraction record's circular wait is an artefact of S-1's REASON: §16.12 blocks *deployment*, §25.1 blocks *exposing*, neither forbids building. ⛔ Standing prohibition on every leaf in the lane. First slice: `mtls.rs` builds a complete `ServerConfig` whose only callers are two test lines. All four B1–B4 decisions are taken in `docs/decisions/2026-09-16_internet-qualification-route.md`. ✅ **`.11.14`'s DECISION IS TAKEN (DOC-0029), and the measurement changed the question.** `migrations/0023:21` declares `UNIQUE (original_locator, expected_digest)`: two tenants citing one URL share one row BY CONSTRUCTION, so the evidence chain is content-addressed by design and a `tenant_id` column would break the dedupe the digest exists for. ⭐ **§16.8 asks for tenant id in every *authorization decision*, not every table** — and that is precisely what is missing. **None of the twelve gains a column**: three gain a tenant-bound READ, the six `evaluation_*` gain a site-operator grant gate, `deployment_targets` is operator infrastructure, and two DEFER to `.6.1.5` by name. 🔴 **Taking the decision found a live path**: `snapshots::stale` carries no tenant predicate and `GET /v1/snapshots/stale` admits on enrolment alone, returning every tenant's `original_locator`, `auth_class` and `provider_receipt`. ⚠️ A disclosure path and an enumeration, not a write path — new owner `.11.14.1`, reproduce first. 🔴 **`.11.15` IS DONE (REPAIR-0212): a split is drawn from a goal line and NOTHING checked it covered one — measured, gate DECLINED, and the one table written found two live defects.** Of 299 leaves, 14 declare a split and **1** carried a mechanism-to-child mapping. ⛔ Requiring it would fire on **13 of 14** — `.11.9`'s rejected shape; comparing a prose child-count to the real one gives false positives by construction, because `.11.9`'s own "three children" is about `.4.1`'s. What ships is `.11.8`'s answer: a tracked, self-tested backlog instrument plus a `TOOLBOX.md` statement, never a gate. ⭐ **The method paid on first use**: `.7.2`'s table found `max_time` declared in `GitLimits`, defaulted to 120 s and **read by nothing**, while R0 enforces the identical field — new owner `.7.2.7` — and three ref-verification clauses owned by the lane with no child, new owner `.7.2.8`. 🔴 The instrument's FIRST number was wrong (0 of 14) and a disagreeing `grep -c` caught an indented-table blind spot. ✅ **`.11.12` IS DONE (REPAIR-0211): every configuration refusal now happens BEFORE the first mutation.** `sqlx::migrate!` ran before `SecretStore::resolve` and before the address parse, both of which are pure functions of the arguments — a typo'd profile against the wrong database left that database migrated and no service running. ⭐ The instrument needs no PostgreSQL: point the server at a port nothing serves, and a boot that reaches the connection reports `PoolTimedOut` while a boot that refuses first reports the configuration it refused. ⛔ The first draft of those controls asserted on a string the failure never prints and PASSED against the defect; the neutralization is what found it. ⚠️ **The bind is REPORTED, not refused**, decided separately as the leaf required: `docs/book/src/deployment.md` documents `rb-server --host 0.0.0.0` as the supported trusted-LAN profile, so a gate would break a shipped capability, and what bounds Internet exposure is G6/G7 with B1–B3. The startup line now names its reach instead of saying `(Phase 0 dev profile)` for every bind (`docs/decisions/2026-09-16_rb-server-bind-exposure.md`). ✅ **`.7.3.5` IS DONE (REPAIR-0210), AND THE FINDING WAS SHARPER THAN THE LEAF WAS OPENED WITH.** This was never "a control is missing": `resolvers.rs::gated_advertises` publishes the R3 pack to every caller with `redirect_policy: "deny"` and `subresource_policy: "deny"`, and the worker enforced NEITHER. ⭐ `a-claim-of-sameness-is-worth-its-call-graph` in a REGISTRY rather than a module header — a header is read by maintainers, an advertisement by callers. 🔴 **Reproduced with a real browser**: the page's `<img>` named `0.0.0.0`, the browser dialed it and the origin served it — `left: 1, right: 0` on the origin's own counter. ⭐ The instrument is `0.0.0.0` again, second use in this tree; a control asserting on the network log would have recorded the browser's INTENT, since that log is written from `EventRequestWillBeSent`. **The repair enforces exactly what is advertised**, at the CDP `Fetch` domain: a document request for a URL the caller asked to navigate to continues, everything else fails with `BlockedByClient` and is NAMED in `refused_requests` on the receipt, while the network log still records the attempt. 🔴 **The behavioural consequence is stated rather than discovered**: a page that assembles its text from an external stylesheet or script renders LESS text than in a desktop browser. That is what "deny" means. ⚠️ Changing the ADVERTISEMENT to `allow` was the alternative and is rejected — it would publish a weaker guarantee for untrusted content than §12.3–12.4's posture. ⭐ **Falsified twice with a real browser**, the second time into the blackout almost-fix, which fails with `net::ERR_BLOCKED_BY_CLIENT` — so the control discriminates a policy from a prohibition. ✅ **`.7.2.6` IS DONE (REPAIR-0209), AND THE CREDENTIAL ARRIVED BEFORE THE REPAIR.** ✅ **`.7.2.6` IS DONE (REPAIR-0209), AND THE CREDENTIAL ARRIVED BEFORE THE REPAIR.** The origin's own record was `[("fetch.test", true), ("second.test", true)]`: the `Authorization` header was DELIVERED to a host the caller never named and the broker never bound. ⭐ **The instrument is two host names on one listener** — an origin is (scheme, host, port), so two names resolving to the same socket make a genuine cross-origin hop with nothing else changed; the log records the `Host` header and whether `Authorization` was PRESENT, never its value. **The rule, decided rather than inherited:** the credential is bound to the origin the CALLER named and is dropped on a hop that leaves it, while the hop is still FOLLOWED — it was re-classified and is a legitimate address, so a redirect to a signed URL on a CDN still succeeds carrying no secret, exactly as browsers and `curl --location` behave. ⛔ Refusing the hop was rejected: it fails closed the same way and breaks the shape the pack exists for. **And the disclosure became a measurement**: `FetchedDocument::credential_hosts` records where the header actually went, replacing `final_url.host_str()`, which named whichever host the redirect chain happened to end on. ⭐ **Falsified twice**, the second time into the almost-fix (`chain.len() == 1`), which passes the cross-origin control and fails the same-origin one — the two controls bound different halves, exactly as `docs/knowledge/neutralize-into-the-fix-you-almost-wrote.md` prescribes two commits after it was written. ✅ **`.11.9.1.3.3` IS DONE (DOC-0027): tranche 4c reconciled, 26 clauses — 9 `handled`, 10 `owned`, 5 `attach`, 2 `unowned` — and it found the mechanism `.7.2`'s own split dropped.** ✅ **`.11.9.1.3.3` IS DONE (DOC-0027): tranche 4c reconciled, 26 clauses — 9 `handled`, 10 `owned`, 5 `attach`, 2 `unowned` — and it found the mechanism `.7.2`'s own split dropped.** ⛔ **AND IT CORRECTS THIS SESSION'S OWN CLOSURE CLAIM.** REPAIR-0208 published "`SIGNOFF-REPAIR.7.2` is now closed on every child"; that is true of the three children DOC-0026's split produced and FALSE of the goal line, which names "validate destinations **and credential forwarding** at every redirect and dial" verbatim. The superseded claim is named here rather than edited away. 🔴 **Tracing the dropped mechanism found a live defect**: `fetcher.rs::fetch_with` re-attaches `extra_header` on every iteration of its manual redirect loop, and `fetch_authenticated`'s only production caller is the R5 arm at `api.rs:2182` passing a broker-resolved credential — so the secret reaches whatever host the origin redirects to, while the disclosure names only the LAST hop. ⚠️ Not an SSRF: the hop IS re-classified, so the destination is a legitimate public address; what crosses is the credential. ⚠️ Source-measured, runtime reproduction pending. ⭐ **The lesson is `MEMORY.md`'s own standing warning — count a goal line's mechanisms against the children a split produces — and it cost ONE COMMIT to relearn**, on the lane whose parent records it. `.3.4` dropped two and was caught by tranche 4b; `.7.2` dropped one and was caught by tranche 4c. Nothing mechanical compares a goal line's conjuncts to a split's children. 🔴 **`.7.3` carries the same shape twice**: "enforce browser redirect/subresource isolation **and credential origin binding**", four children, neither carried. The first is live and measured — the R3 pack pre-flights ONE url and hands the page to a browser that RECORDS every request and refuses none (no `Fetch.enable`, no `setBlockedURLs`), while R0 and R1 classify every hop — new owner `.7.3.5`. ⛔ The second is a population of ZERO (`grep` over the browse crate returns no credential, cookie or `Authorization`) and deliberately gets NO leaf, as `.3.4.1` refused to enforce a rule over an empty population. 🔴 **And the site-global data model is a position nobody has taken**: twelve of twelve policy, evaluation, deployment and evidence tables carry zero `tenant_id` columns, `.6.1.5` owned exactly one of those questions, and answering them one surface at a time is how a data model gets decided by accident — new owner `.11.14`. ⭐ `R-36-39-2` is wholly `handled`, all four clauses, and `git show 9c2d2ba` proved it STALE rather than false: the baseline loaded the boundary by TARGET TENANT and the grant by `ORDER BY valid_from DESC LIMIT 1`, both verbatim. ✅ `.7.2.5` IS DONE (REPAIR-0208): the acquisition fixtures had kept the `Permissions::all()` open `.7.2.4` removed from production, so the controls built their own inputs out of the host's git configuration. `.7.2.5` IS DONE (REPAIR-0208): the suite had kept the `Permissions::all()` open `.7.2.4` removed from production, so the acquisition controls built their own fixtures out of the host's system, application and user git configuration. ⭐ **The reproduction cost one line**: the child-process probe `.7.2.4` left behind printed `acquisition HEAD: ref: refs/heads/main` beside `fixture HEAD: ref: refs/heads/smuggled`, one process, one environment, production refusing what the suite accepted. ⚠️ No assertion had moved — the fixtures assert commit ids, counts and refusal names — so this is a REPRODUCIBILITY defect and the leaf says so rather than inflating it. ⛔ The probe's own `gix::init_bare` is deliberately retained and its exemption is stated in the source, because a control whose every open refuses the setting cannot tell a refusal from a setting that never arrived. ✅ **`.7.2` IS CLOSED ON ALL THREE SPLIT CHILDREN — `.7.2.4` IS DONE (REPAIR-0207): the acquired repository is opened with `gix::open::Options::isolated()`.** The census was owed before the fix and came out of gix 0.87.1's own source: `gix::init_bare` is `ThreadSafeRepository::init(…, open::Options::default_for_level(Trust::Full))` → `Permissions::all()`, which admitted the system config, the `$XDG_CONFIG_HOME` application config, `~/.gitconfig`, `GIT_CONFIG_COUNT`-sourced config, `include`/`includeIf` directives reaching outside the repository, the system and application `gitattributes`, and every `GIT_*`/`SSH_*` environment category — while fetching a CALLER-SUPPLIED URL. 🔴 **Reproduced first, and the leak is real**: with the opener reverted, a setting supplied only through the process environment landed in the acquisition's own repository — `acquisition HEAD: ref: refs/heads/smuggled`. ⭐ **The instrument is a child process**, because `GIT_CONFIG_*` cannot be set in-process without mutating state every parallel test shares; the probe opens one repository the superseded way and one the production way under the SAME environment, so the first must say `smuggled` (the setting really arrives) and the second `main`. ⛔ Without that first half a repair that broke the plumbing would look identical to one that refused it. ⛔ **This is what `.7.2.1` could not reach**: owning WHERE gix writes says nothing about WHAT gix reads. ⚠️ The transport is a custom `gix_transport` client, so `http.*` never reached the dial and the repair does not claim to change that. ✅ **`.7.2.3` IS DONE (REPAIR-0206): the Git LFS gate is anchored to the pointer's own grammar, and the policy it enforces is now STATED.** ✅ **`.7.2.3` IS DONE (REPAIR-0206): the Git LFS gate is anchored to the pointer's own grammar, and the policy it enforces is now STATED.** The gate refused a blob when the ten-byte run `version ht` appeared anywhere in its first 64 bytes, so a repository that so much as DOCUMENTED Git LFS could not be acquired — reproduced first, with two controls red: a documentation file quoting the version line in backticks and a manifest for an unrelated specification were both `Refused { what: "Git LFS" }`. ⚠️ The defect ran in the OVER-refusing direction — availability and correctness, never a bypass, and the leaf says so rather than dressing it up. The repair is a 42-byte prefix test at offset 0 that must end at a newline, because the specification makes the `version` line the pointer's first line; `oid`/`size` are deliberately not required, since a truncated pointer is still not the content. ⭐ **Falsified in two directions, because the repair has two halves**: restoring the search fails both controls, and an anchored-but-not-spec-matched `starts_with(b"version ht")` passes the prose control and fails the other one. ⚠️ The acceptance's own "41-byte prefix" was re-derived at **42** and corrected in place rather than used. The policy — REFUSE, because a pointer's bytes were never transferred, never classified and never counted against a budget — is now in `docs/book/src/deployment.md`, where ROADMAP §12.5's "explicit Git LFS policy" can be checked against it. ⭐ **`.7.2.2` IS DONE (REPAIR-0205) — the last §16.12 line that was still an OPEN defect is repaired, and it was REPRODUCED first.** With the unrepaired policy restored the control fails with `Ok(200)`: the client FOLLOWED a redirect to `0.0.0.0` and the refused destination served the request. ⭐ The instrument is `0.0.0.0` itself — it classifies as `reserved` and the kernel routes a connection to it at the local host, so the same origin answers and its hit counter reports whether the hop was DIALED; a control that only checked for an error could not tell a classification from a connection that happened to fail. ⚠️ `127.0.0.2` was the obvious first choice and does not bind on this host (`Errno 49`), measured. The repair puts the pre-flight's own policy in the redirect decision, re-states the five-hop cap that `Policy::limited` used to own, and carries a typed refusal out of band because reqwest's policy can only answer follow/stop/error. The DNS belt now NAMES a refusal instead of returning an empty address list. Two residuals recorded rather than absorbed: the POST path has no error channel for a refusal, and an `https → http` hop is still unrefused. ⭐ **`.7.2` is CENSUSED AND SPLIT (DOC-0026) into the three repairs its goal line was carrying** — a redirect policy, a content predicate and how a repository is opened — each with its own acceptance, because `.11.13`'s rule is that a leaf without one is not an owner. The census NARROWED the published finding: a hostname hop IS covered by the DNS belt, so the uncovered destination is precisely an IP-literal hop. row 1a is `.11.9.1.3.3`, tranche 4c. ✅ **`.3.4` IS CLOSED (DOC-0025): the whole delegation lane, eight children, reconciled against its GOAL LINE rather than against their statuses — and three of its six verdicts are not "done".** Delegability is REFUSED as a gate (`delegable` governs grant chains that have no producer), bounded depth is deliberately unenforced over a population of zero, and consent is NOT A REQUIREMENT. Three residuals are carried forward by name: chains and their depth bound, `lease_expires_at` received and never read, and a delegation's attribution claim on a subject that never agreed. ✅ **`.3.4.7` IS DONE (DOC-0023) and the answer is sharper than the question.** The subject is NOT asked, deliberately — and **consent was never a delegation requirement in this project**: `grep -ic consent ROADMAP.md` returns 8 and **zero** of them fall inside §16.3 (lines 1754–1768), where the delegation invariants live and where six are stated without one. The roadmap's consent is §4.4's — an ENROLLMENT and mandate-domain act carried on the authority boundary as `target_disclosure_and_acknowledgement`. ⛔ So ADR-009 was silent because there was nothing to decide, not because a decision was skipped. The residual is bounded to ATTRIBUTION — a delegation reaches nothing the actor could not reach alone — and each of the four gates that bounds it is pinned by a NAMED control, so no new one was added for appearance. ⭐ Both mechanisms `.3.4`'s own split dropped are now carried. ✅ **`.3.4.6` IS DONE (REPAIR-0204): the idempotency hash now binds the command's TARGET.** Reproduced before repairing, and the observed answer was worse than the prose — the second request, same actor and body and key against a DIFFERENT thread in the same tenant, answered `200 replayed=true` carrying the FIRST thread's `thread_id` and `event_id`. A caller addressed one thread and was handed another thread's event as its own result. ⛔ The target is bound exactly where a caller can vary it independently of the key (the eleven operations of `POST /v1/threads/{thread_id}/commands`); three other callers pass none, each with a structural reason — a creation has no thread and its target is the tenant, the MCP tool fixes the thread inside its key, and a node result is keyed by the server's `command_id`. ⚠️ The migration answer is NOT `.3.4.2`'s: every historical key for those eleven operations now CONFLICTS instead of replaying, which is the safe direction, and the other three keep replaying byte-identically. ✅ **THE WORKSPACE IS GREEN IN ONE COMPLETED RUN (`.11.4.8`, REPAIR-0203): 103 suites, 816 passed, 0 failed**, rc=0, with zero skips — and against `.11.4.7.2.2`'s 815 passed / 1 failed on the same 94 binaries, exactly one test moved. 🔴 The one red test was never a product defect (`.11.4.7.2.4`, REPAIR-0202): the browse test support DISCOVERED a desktop browser whenever `R3_BROWSER_BIN` was unset, so a control qualified against the pinned Chrome for Testing runtime was being evaluated against the browser that pin exists to avoid. **A pin a second path can bypass is not a pin.** 🔴 **G6–G7'S SEVEN SHIPPED §16.12 LINES ARE RE-DERIVED (`.11.4.7.1`): THREE MUST BE RE-EARNED, TWO NARROWED, TWO STAND.** ⛔ The gate's conclusion is UNCHANGED — still NOT MET for Internet exposure — and the Phase-7 record is byte-unchanged; the new record ADDS to it. ⭐ "Since" is exact: the gate closed 2026-09-08, the earliest corrective repair is 2026-09-09, and comparing every repair's timestamp against the record's returns ZERO that predate it. 🔴 **Line (4) is the only one still an OPEN defect, measured as a two-site contrast inside one product**: `fetcher.rs` sets `Policy::none()` and re-classifies every redirect hop manually, while `git.rs` sets `Policy::limited(5)` and relies on a DNS hook behind the hops — and hyper-util 0.1.20 says in its own source that it skips the resolver when the host is already an IP address. The R1 header meanwhile claims R0's property. ⛔ **CORRECTED the same day**: first published as "its only destination control" and as covering the initial URL; `GitFetcher::classify` is a real PRE-FLIGHT that handles an IP literal explicitly, so the defect is the **redirect hops alone** and the exposure is an origin that REDIRECTS into a private range, not a caller naming one. ⚠️ Source-measured, runtime reproduction pending; owner `.7.2`, record `R-44-45-1`. ⚠️ **Re-earning (2), (3) and (8) is NOT repair work** — those defects are fixed; what is missing is the COVERAGE measurement that would let each line be counted again. Four gate records remain (`.11.4.7.2`–`.4`).  🔴 **CAN THIS PROJECT STILL STATE ITS OWN RELEASE-GATE POSITION?** `.11.4.7` is censused and split four ways: **35 verdict claims and 19 deferrals** across FIVE gate records — G1–G2, G3, G4, G5, G6–G7 — every one counted BEFORE the full source read. ⛔ The records are not dishonest: each claims the evidence its suites then carried, and what was never measured is those suites' COVERAGE. G6/G7 line (2) is *tenant isolation* and five repairs have since reproduced defects inside it (`.6.1.1`, `.3.5.3`, `.3.3.4.10.3`, `.6.1.2`, `.9.2.1.2`). 🔴 **The census found a defect before any re-derivation ran, and it is ARITHMETIC**: the Phase-1 record says "five named deferrals" twice and its own table lists SIX, and `git log -S` shows the sixth row shipped in the SAME commit as the sentence — wrong when written, not overtaken. The book repeated it and is corrected; the record is superseded rather than edited (`.11.4.7.2`). 🔴 **And a gate cannot see the claim it was built for**: `check_visibility_policy.sh` anchors all four of its sentence shapes on the full word `repositor(y\|ies)`, so two live instances written with the abbreviation are invisible to it — both correct HISTORY, which is exactly why the gap survived (`.11.2.4`). ⭐ The gate proved PRECISE by refusing the leaf that documented it.  🔴 **Tranche 4b (DOC-0021) found it, and the finding is about `SIGNOFF-REPAIR.3.4` itself**: that leaf is `active`, all FIVE of its children are `done`, and TWO of its own goal line's mechanisms have no child — "actor/subject participation **and consent**; bind replay hashes **to target** and authority context". Its split bullet says "five children along the four mechanisms the goal line names"; the goal line names five, and nobody checked the sentence's arithmetic. ⭐ That is a THIRD shape of lost ownership the ledger had not recorded — not a leaf that never cited a record, and not `attach` (a goal line that does not make a clause visible), but a goal line naming the mechanism VERBATIM whose own split dropped it; the closed state set is deliberately not extended, because the action is `unowned`'s. New owners `.3.4.6` and `.3.4.7`. 40 clause rows: 16 `handled`, 11 `owned`, 7 `attach`, 4 `unowned`, 2 `declined`; `--classified` rc=0 at 244 rows with all 47 `attach` clauses named by their leaves. 🔴 Four further live findings, each routed rather than reported: a role can **auto-initiate exactly once per tenant, for ever** (`create_thread_auto`'s key is `format!("auto_{}_{}", role, tenant_id)` against an `idempotency` table keyed `(tenant_id, idempotency_key)`) — annotated at `.5.2`, whose goal line already says "make repeated legitimate auto initiation possible"; the R1 Git acquisition **opens its repository with gix's DEFAULT permissions over an untrusted remote** while gix 0.87.1 ships the unused `open::Options::isolated()` — attached to `.7.2`, and distinct from `.7.2.1`, which bounded where gix WRITES; **the quarantine gate does not exist at all** (`quarantine_status` has ZERO occurrences under `crates/*/src`, and the §9.8 code `evidence_quarantined` is constructed nowhere) — attached to `.7.4`; and `.3.3.4.12` **left the three superseded federation direction services standing with no caller** while the book now contradicts itself about them in one chapter — new owner `.3.3.4.12.2`. ⭐ Two clauses were REFUTED at HEAD and both were right when written, told apart by `git show` at the review baseline. ⭐ `.9.3.4`'s DECISION is taken (DOC-0020) and measuring it REVERSED the pre-announced answer: the leaf's premise that `GrantAction`/`TargetSelector` are frozen under §9.8 is false — §9.8 is the reason-code registry and `git grep -c` over `ROADMAP.md` returns **0** for both. `GrantAction` EXTENDS with administrative verbs; `TargetSelector` does NOT, because `Threads { threads }` enumerates objects at grant time and the ordinary flow publishes a publication whose id does not yet exist. 🔴 The extension is a **migration**: boundaries store wire names, so no existing row can contain a new action and the verb stops working for every enrolled tenant unless a disposition is chosen. Decomposed into `.9.3.4.1`/`.9.3.4.2` with acceptance; not implemented (207 references, 19 files). 🔴 `.9.2.1` is CLOSED, all three children: REPAIR-0192 closes `.9.2.1.2`, where both publish verbs admitted **any enrolled principal** — enrolment in any tenant was the whole predicate for writing a publication into a Git repository and for declaring it effective. One definition both verbs call now requires an `owning_authority` the caller HOLDS (`authority::grant_held_by`, `.9.3.1`'s predicate), checked before the path is resolved and before the publication is loaded. ⭐ Grant ids are derivable from a principal id, so the load-bearing leg is naming SOMEONE ELSE'S grant — refused — beside the matched pair where only the grant's holder differs and the request is admitted. ⚠️ **The limit is recorded, not papered over**: no grant action and no target selector can NAME a publication, so a held grant is effectively TENANT-WIDE for these verbs, and the book says so rather than implying the verb is scoped; `.9.3.4` owns the narrowing and is promoted to frontier row 1b. 🔴 `.9.2.1.3` is closed by REPAIR-0191: `mark_effective` recorded whatever Git object ids the caller declared and nothing opened a repository, and **three of this project's own fixtures drove the transition with `abc123`** — a suite qualifying the transition with ids that do not exist. `publisher::missing_objects` now answers per id, in the CORE both publish verbs go through rather than in either handler, and `resolve_repository` returns a `PublicationRepository` newtype with no other constructor, so containment became a type rather than a convention. The three fixtures are RE-SEEDED with ids read back from a real repository, not relaxed. ⚠️ `repo_path` is now required on the effective verb — a wire-contract change, because a transition that cannot say which repository it means cannot check anything. ⚠️ Limit stated: an id is proved to EXIST, not to be the publication's own; binding it to the publication's refs needs the repository recorded with the row. 🔴 `.9.2.1.1` is closed by REPAIR-0190: the publish verb took its repository location from the REQUEST BODY and handed it to `gix::open`, so any enrolled principal named any path on the server's filesystem. The deployment now declares one root, the caller names a location inside it, and both sides are CANONICALIZED before they are compared — which is why `..` and a symlink out of the root are refused by the same test, the symlink case being the one a string comparison admits. An undeclared root closes the verb with a new `503 publication_repository_unconfigured`, and an unusable declared one refuses the boot before the migrations run. ⚠️ The repair exposed a pre-existing fixture defect it was told to preserve: the "non-repository refuses" leg ran after the publication was already `effective`, so the STAGE check answered it and `gix::open` was never reached — green for the wrong reason for as long as it has existed. 🔴 `.9.2.1` was DECOMPOSED by REPAIR-0189 into three children: its three acceptance clauses are three different repairs — one invents server configuration and a containment predicate, one changes the wire contract of two shipped verbs, one adds a repository read to a database transition — and each needs its own control observed RED before its fix. 🔴 `.11.2.3` is closed by REPAIR-0187: the arity gate modelled the OPPOSITE of the renderer — it treated a pipe inside a code span as part of the cell, and its own `--self-test` asserted that same false answer, so the corpus read **0** defective rows in 323 files while mdbook was dropping cells. The deliberately-retained row was the proof: it published **516 of its 1,027** rationale characters and a bare `—` where its enforcer name belongs. All nine arms are now the renderer’s verdict, rendered before being asserted. 🔴 `.6.1.2` is closed by REPAIR-0185: the leaf named the MCP seam and the defect was in the SHARED CORE, where the HTTP verb takes no tenant at all and was equally open — a seam-level repair would have left it open and the seam's own suite green. 🔴 `.9.3.1` is closed by REPAIR-0184: citing an authority was the same as holding one on THREE surfaces, not the two its leaf named, and no site in the family had ever consulted `valid_from`. 🔴 `.6.1.1` is closed by REPAIR-0182, the severest defect this reconciliation produced: the three MCP read tools ran a PRIVATE re-implementation of an authorization their own module header said they shared, and a live control measured a caller in one tenant receiving another tenant's entire thread projection. The three reads now CALL the HTTP handlers' own gates through the new `mcp_read_internal` seam; the copy is deleted. ⚠️ **One part of the finding was corrected by measurement rather than confirmed**: the policy registry has no tenant column and no site filters by one, so the bundle was never crossing a tenant boundary — it is site-wide on both surfaces, and the tool's real debts were the enrolment gate and a false tenant label. The schema question is opened as `.6.1.5`. Tranche 4c is row 1c | repo-local |
| [`PHASE-8`](tasks/PHASE-8.md) | `active` | corrective prerequisite `SIGNOFF-REPAIR`; then `.5.3` — the store-and-forward (opened at the Phase-7 close; **the `.1`–`.4` lanes COMPLETE**; `.5` the regional-routing census → decomposed `.5.1`–`.5.4`; `.5.1` ADR-035; `.5.2` the regional routing) | repo-local |
| [`PHASE-9`](tasks/PHASE-9.md) | `proposed` | `.1` — stable release | repo-local |
