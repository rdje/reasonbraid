# Architecture decision records

ADRs live here, one file per decision. Copy `TEMPLATE.md`. Status, context,
options, evidence, choice, consequences, and a revisit trigger are required
(`ROADMAP.md` §23). No ADR is accepted merely because the roadmap names a
candidate technology.

| ADR | Title | Status | Leaf |
| --- | --- | --- | --- |
| [001](001-uncleared-working-name.md) | Uncleared working name | accepted (internal only) | `PHASE-0.0.1` |
| [002](002-phase1-scope.md) | Phase 1 scope: GO on the LAN vertical slice, single-agent-default routing | accepted (signed by the accountable owner 2026-09-06) | `PHASE-0.8.2` |
| [004](004-postgresql-aggregate-outbox.md) | PostgreSQL aggregate/event/outbox pattern: locked head, claim-first, one-transaction writes, in-server library | accepted (evidence-gated) | `PHASE-1.1.1` |
| [005](005-event-transport-postgresql-queue.md) | Event transport: the PostgreSQL queue, accepted with evidence (the WP2 leased outbox worker + ADR-004 + the ADR-006 pull channel — shipped across two phases; the broker trigger is named) | accepted (evidence-gated) | `PHASE-2.2.1` |
| [006](006-node-transport-reconnect.md) | Node transport and reconnect protocol: the authenticated outbound channel, accepted with evidence (promotes the WP3 + `.1.2.2` channel decisions) | accepted (evidence-gated) | `PHASE-2.1.1` |
| [007](007-workload-identity-issuance.md) | Workload identity issuance: a project-local CA with short-lived certificates (the spike measured rcgen + rustls over a real TLS 1.3 handshake; step-ca/SPIRE evaluated, not installed) | accepted (evidence-gated) | `PHASE-2.1.1` |
| [008](008-authorization-engine-and-cached-decisions.md) | Authorization engine and cached-decision semantics: the shipped in-tx evaluator stays (accepted with evidence); the node caches ONLY the admission decisions riding its delivery — 60s freshness, a revocation epoch bump invalidates, the §16.4 fail table governs the unreachable store | accepted (evidence-gated) | `PHASE-2.1.5.1` |
| [009](009-delegated-authority-representation.md) | Delegated authority representation: chain-in-envelope for the dev profile (the plumbing was pre-shaped; the widening invariant is a pure, tested function; the wire form beats a token blob) | accepted (evidence-gated) | `PHASE-2.1.4.1` |
| [012](012-provider-attempt-ambiguity-retry-policy.md) | Provider-attempt ambiguity and the per-adapter retry policy, accepted with evidence (the WP3 boundary-first journal + the WP4 prove/adjudicate exits + the `.2.3` pure retry classes; the risky re-run requires the explicit possible-duplicate authorization) | accepted (evidence-gated) | `PHASE-2.3.1` |
| [015](015-recruitment-policy-baseline.md) | Recruitment policy baseline, accepted with evidence: the shipped `.1.3` explicit invitation (the human names the participants, the invitation is the real capability, dispatch-on-accept) PROMOTES as the recruitment baseline; the open call consumes the matching lane's candidates; the dependence indicators land with `.6` (the trigger named); the storm controls gate both paths | accepted (evidence-gated) | `PHASE-3.4.1` |
| [011](011-object-store-content-addressing.md) | Object store and content-addressing format, accepted with evidence: `sha256:<hex>` over the ACQUIRED bytes (as acquired, pre-transformation) pins the references' `expected_digest` AND the future snapshots to one scheme; the store's layout + the derivation graph ride the `.6` snapshots lane behind the first-snapshot-receipt trigger | accepted (evidence-gated) | `PHASE-4.1.1` |
| [018](018-resolver-sandbox-isolation.md) | Resolver sandbox/runtime and network isolation, accepted with evidence: the isolation CLASS vocabulary (the sandbox-level ladder `none` < `process` < `constrained_process` < `vm_container` + the egress class `none`/`loopback`/`listed`/`any`, the claim is the maximum) pins the registry's advertise shape; the runtimes ride the resolver packs; a required-isolation miss is the explicit failure, never the silent downgrade | accepted (evidence-gated) | `PHASE-4.1.1` |
| [014](014-directory-semantic-index.md) | Directory semantic-index engine and embedding lifecycle, accepted with evidence: the §10.3 stage-1 eligibility is structural by the roadmap's own table and evaluated deterministically; the embedding engine arrives behind its trigger (the first measured under-selection) in shadow mode — never an authorization control; the profile schema carries no embedding columns | accepted (evidence-gated) | `PHASE-3.1.1` |
| [022](022-audit-hash-chain-groundwork.md) | Audit hash-chain: the shipped linkage (the actor/subject/grant/digest bindings, the deterministic handles, the per-thread monotonic sequence, the audit reconstruction) is the chain's groundwork, accepted with evidence; the hash chain + checkpoint + verification policy are deferred WITH their trigger (the first non-loopback deployment / the G7 ops gate) | accepted (evidence-gated) | `PHASE-2.7.3` |
| [016](016-workflow-profiles.md) | Workflow profiles: versioned configuration over the same aggregates — composition never creates capability | accepted (evidence-gated) | `PHASE-5.1.1` |
| [017](017-evaluation-service.md) | The evaluation service: records and gates, never a second judge — the harness measures, the service persists and blocks (the versioned digest-pinned registry, the seed-declaring experiment records, the shadow-only routing trials, the recorded cohorts, the accumulating calibration, the blocking-only G5 gate) | accepted (evidence-gated) | `PHASE-5.4.1` |
| [029](029-structured-deliberation.md) | Structured deliberation: typed disagreement over the same verbs — the records shape the wire, never a new capability (the typed claim/objection/revision, the blind commitment point as a read-surface rule, the evidence request riding the Phase-4 pipeline, the attributable verdict, the minority report's coverage report, the §13.4 twelve terminals) | accepted (evidence-gated) | `PHASE-5.2.1` |
| [030](030-moderation-and-synthesis.md) | Moderation and synthesis: the prohibition is the vocabulary's negative space — the moderator gets no new verbs (the moderation actions are contributions with a closed kind set, the capability-shaped fields refused, the appealable action is the existing challenge, the `moderate` step joins the vocabulary, the synthesis record is re-derivable derived content) | accepted (evidence-gated) | `PHASE-5.3.1` |
| [031](031-routing-policy.md) | The routing policy: deterministic rules with the human authority outranking them — the learned routing is a shadow recommendation, never a raise (the case class is a submitted input, the rule table is a lookup, the explicit profile wins, the recommendation names only existing arms and is recorded, never applied) | accepted (evidence-gated) | `PHASE-5.5.1` |
| [019](019-semantic-policy.md) | The semantic policy: a versioned digest-pinned document with the authority in the grants, never the label — the resolution is seven steps and fails closed (the typed `PolicyVersion` + the `PolicySetVersion` lock manifest, the ownership = the authority binding, the explanation tree rides the result) | accepted (evidence-gated) | `PHASE-6.1.1` |
| [032](032-policy-lifecycle.md) | The policy lifecycle: five records that never fold — the discussion rides the thread, the approval carries its authority proof (the proposal references the policy version + the thread, the decision freezes the electorate snapshot, the approval re-checks the grant at the action time) | accepted (evidence-gated) | `PHASE-6.2.1` |
| [033](033-projection-compiler.md) | The projection compiler: byte-identical output from the resolved clauses — the unrepresentable clause names itself, never silently omitted (the compiler renders the resolved set, never re-resolves; the hermetic separate crate; the projection digest is the publication's verification primitive) | accepted (evidence-gated) | `PHASE-6.3.1` |
| [034](034-internet-hardening.md) | The Internet hardening: capability per the qualified surface — the transport is context, never identity, and the isolation is defense in depth (the per-surface qualification, the certificate-fingerprint identity, the RLS second layer, the quota vocabulary over the budget machinery) | accepted (evidence-gated) | `PHASE-7.1.1` |
| [027](027-signing-and-distribution.md) | Plugin/adapter signing and distribution: the release identity signs the digest-pinned manifest, and a downloaded adapter verifies against the ordered fail-closed ladder (allowlist → digest → signature → API compatibility → capability manifest); the distribution channel + the reproducible builders are the named deferrals | accepted (evidence-gated) | `PHASE-7.2.1` |
| [026](026-federation-trust-agreement.md) | The federation trust agreement: explicit, named, and local-grant-gated — the agreement record is the federation's single capability source (never a transitive default); the remote domain vouches for its own records, the local grant is the ONLY authority that acts locally (the kill line as the invariant); the receipts cross-reference never merge; the cards verify through the ADR-027 ladder | accepted (evidence-gated) | `PHASE-8.1.1` |
| [025](025-a2a-interoperability-surface.md) | The A2A interoperability surface: a facade with recorded semantic losses, pinned and demonstrated — the facade maps the compatible semantics (the message is an INPUT the local machinery evaluates); each exchange records its losses (the authority/budget/evidence/decision-rule/lifecycle — never a silent merge); the exact crate versions pin + the tested conformance revision + the roundtrip demonstrate the pre-1.0 compatibility; the qualification precedes the broad claims | accepted (evidence-gated) | `PHASE-8.2.1` |
| [020](020-canonical-publication.md) | The canonical publication: a nine-step state machine over the staged record — the Git refs are the publication truth, the reconciler never silently promotes (the typed staged/effective/failed states, the compare-and-swap idempotency, the six-row §15.8 matrix) | accepted (evidence-gated) | `PHASE-6.4.1` |
| [021](021-target-deployment.md) | The target deployment: per-target waves, never globally atomic — the receipt attests the digest, the correction authorities stay distinct (the desired/observed pair, the six-way drift vocabulary, the §4.7 operations with their own authority proofs) | accepted (evidence-gated) | `PHASE-6.5.1` |

| [023](023-telemetry-storage-redaction.md) | Telemetry storage and redaction: the four records are separate systems (the shipped eprintln-vs-audit-tables separation); the §18.2 redaction rules pin the FUTURE sink (never prompt text/credentials/model output in span attributes, sensitive IDs tokenized at the boundary); the OpenTelemetry dependency waits for the trigger | accepted (evidence-gated) | `PHASE-2.5.1` |
| [013](013-budget-units-settlement-invariants.md) | Budget units and settlement invariants, accepted with evidence (the WP5 engine: reserve before dispatch at both boundaries, settle with actual usage, overruns reported never clamped, holds on indeterminate attempts; pricing snapshots deferred to Phase 4+) | accepted (evidence-gated) | `PHASE-2.3.1` |
