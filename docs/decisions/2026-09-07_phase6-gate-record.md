# Phase 6 gate record — G3 (`PHASE-6.7.2`)

## Context

`PHASE-6.7.2` (the G3 exit gate package, the Phase-1 `.1.8.2` /
Phase-4 `.7.2` / Phase-5 `.6.2` pattern): Phase 6 ships the policy
and doctrine governance programme — the semantic policy schema
(`.1`), the lifecycle records (`.2`), the deterministic compiler
(`.3`), the signed publication (`.4`), the target deployment
(`.5`), the outcome monitoring (`.6`). The gate: `ROADMAP.md`
§20.6's **G3** — authority/consent/quorum/publication/correction
tests, blocking the BINDING POLICY USE. The kill/pivot line
(`ROADMAP.md` §25.1): do not ship binding policy governance
unless the real owners accept the authority/correction model.

## Decision

- **G3 outcome: Met as machinery, blocked as binding use — the
  block is discharged by subtraction.** The gate's TESTS are
  green (the `.7.1` census's clause map): the authority (the
  `.2` grant re-checks at the decision/approval/correction
  boundaries + the `.1.2` ownership binding — policy 4/1), the
  consent (the Phase-1 invitation-response machinery), the
  quorum (the electorate snapshots frozen at the action time —
  policy 3), the publication (the `.4` nine-step machine + the
  compare-and-swap + the six-row reconciliation — policy 7/8 +
  publisher 2 + reconciler 3), the correction (the `.5.3` §4.7
  operations with the authority proofs — policy 10). The
  BLOCKER — the binding policy USE — is NOT claimed: the exit
  claims the machinery and the records; the real owners'
  acceptance of the authority/correction model is the OPEN
  kill/pivot condition, named in the subtraction record.
- **Demonstration B ships as the nine-step walk**
  (`docs/evidence/2026-09-07_demonstration-b.md`): each of the
  §26.2 steps maps to the shipped records + the tests (the
  census's mapping, rendered) — the walk is the differentiator's
  evidence, not a live demo (the live provider runs are the
  env-gated qualification surface, the WP7 precedent).
- **The named deferrals** (each recorded, none silent):
  1. **The binding policy use**: no deployment claims a binding
     policy — the governance machinery is demonstration-grade
     until the owners accept the model (§25.1).
  2. **The real-owner acceptance**: the authority/correction
     model has no external owners yet (the repo is private) —
     the acceptance is the named condition, not the exit's
     claim.
  3. **The G7 publication portion**: the publication machinery
     ships + the reconciliation tests; the G7's FULL publication
     gate (the Internet-qualified profiles) rides the PHASE-7
     lane.
  4. **The remote publication profile**: the `.4.3.1` store is
     the LOCAL bare repository; the remote write surface waits
     for the G7/PHASE-7 work.
- **The subtraction record** (`2026-09-07_phase6-subtraction-
  record.md`) lists the deferred claims — no empty lists.

## Consequences

- The tree closes (`PHASE-6` `done`), the frontier moves to
  `PHASE-7.1` (the Internet-hardening lane).
- The binding-policy claim stays WITHDRAWN until the owners'
  acceptance exists — the G3 gate's blocking function is
  discharged honestly, not by fiat.

## Indexed under

- `PHASE-6.7.2` (the owning leaf).
