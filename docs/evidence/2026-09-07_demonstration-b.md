# Demonstration B record (`PHASE-6.7.2`; ROADMAP §26.2)

- Date: 2026-09-07 · Repository revision: the `.7.2` close commit (this
  record ships in it)

The §26.2 walk — the governed doctrine change over the SHIPPED machinery.
Each step names its records + its tests (the `.7.1` census's mapping). The
walk is the differentiator's evidence; the live provider runs remain the
env-gated qualification surface (the WP7 precedent).

| §26.2 step | The shipped machinery | The evidence |
| --- | --- | --- |
| 1. The agent-initiated proposal | the `.2` proposal records (the policy version + the thread reference) | policy 3 (`the_proposal_and_the_decision_stay_separate_records`) |
| 2. The directory recruitment without the prior knowledge | Phase 3's recruitment protocol (the dependence indicators + the panel calls) | the Phase-3 suites (profiles 12/13-era) |
| 3. The HTTPS + Git evidence with the immutable snapshots | Phase 4's R0/R1 packs + the snapshot store (the digests over the acquired bytes) | profiles 15/16/19 |
| 4. The challenge + the minority report + the impact map + the rollout recommendation | the Phase-5 challenge verb + the minority report riding the close + the `.1.3` impact maps | profiles 28; policy 2 |
| 5. The decision rule + the SEPARATE human approval | the `.2` decision (the frozen electorate + the verdict) + the approval (the grant re-check) | policy 4 |
| 6. The byte-reproducible projections with the explicit loss | the `.3` compiler (the byte-identical + the declared unrepresentable) | compiler 8 + policy 5/6 |
| 7. The crash-surviving publication | the `.4` nine-step machine + the compare-and-swap + the six-row reconciliation | policy 7/8; publisher 2; reconciler 3 |
| 8. The canary deployment + the coverage | the `.5.2` per-target waves + the receipts (the desired/observed pair) | policy 9 |
| 9. The adverse outcome → the suspension + the supersession + the historical queries | the `.5.3` corrections + the audit reconstruction (the event log's history) | policy 10; the Phase-1 audit suites |

## The G3 clause map

| G3 clause | The lanes' suites |
| --- | --- |
| the authority | the `.2` grant re-checks (policy 4) + the `.1.2` ownership (policy 1) |
| the consent | the Phase-1 invitation-response machinery |
| the quorum | the electorate snapshots (policy 3) |
| the publication | the `.4` lane (policy 7/8 + publisher 2 + reconciler 3) |
| the correction | the `.5.3` lane (policy 10) |

## The §25.1 reading

The G3 blocker — the BINDING POLICY USE — is NOT claimed: the walk
demonstrates the machinery; the real owners' acceptance of the
authority/correction model remains the named kill/pivot condition (the
gate record + the subtraction record carry it).
